# Decisions & Docs Index

A short index of the durable restructure (D-1 / D-2 / D-3 / D-4 / D-5) documentation, and — critically — a
pointer to what is **settled** vs. **still open**, so the next reader doesn’t re-litigate closed
decisions or miss the live thread.

The `PRD-D-*` series **is the current product canon** (the “v12 line”) and supersedes the 10.25/v11.x PRDs.
The older specs are archived under `_docs/archive/` for history: `PRD-v11.2.md` (the last v11 diff, with
v11.3 / v11.4 / v11.5 folded in place — there is no standalone v11.3+ file), `PRD-v11.1.md`, `PRD11.md`,
and `Joshing_PRD_v10_25 (1).md`.

**A “Settled” decision is one that has been *made*, regardless of build state.** Each settled entry carries
a build tag so a made-but-unshipped decision is never mistaken for live behavior:

- `[built]` — live in the default code path.
- `[built, flag-off]` — code merged but gated behind a flag; computed, not yet adopted.
- `[decided, NOT built]` — the call is made; the code does not yet reflect it. **Live behavior still differs.**
- `[built, specced≠as-built]` — something shipped, but it diverges from the decision as written; reconcile pending.

Decision state and build state are tracked independently. The build tags below were verified against live code
by `D-DECISIONS-CONFORMANCE-01` (2026-06-13); where the audit refuted an earlier assumption, the entry says so.

Last updated: 2026-06-15 (D-ACCOUNT-DELETION-TERRITORY-01 ratified — account deletion tombstones authored questions with retained-user territory, preserves answerers' proven evidence, removes author credit, hard-deletes the departed user; decided, NOT built. Earlier same day: D-HOME-DASHBOARD-MODEL-01 — Home is a bounded 7-day dashboard, superseding D-HOME-PACING-01 for Home).

## Durable docs

|Doc                                                  |What it is                                                                                                                                                                            |
|-----------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|`PRD-D-0-PRODUCT-DIRECTION-AND-DECISIONS.md`         |Canonical “what the product is and why”: target capabilities, deferred items, open hypotheses, **amended decisions**.                                                                 |
|`PRODUCT-CANON.md`                                   |Designer-facing product canon. (Stale in §8/§9 as of 2026-06-13 — see canon-edits note at the bottom.)                                                                                |
|`PRD-D-1-FEED-DAILY-RESTRUCTURE-SPEC.md`             |Spec — directional follow + Feed/Daily split.                                                                                                                                         |
|`PRD-D-2-NICHE-MATCH-DISCOVERY-SPEC.md`              |Spec — niche-match discovery engine.                                                                                                                                                  |
|`PRD-D-3-HOUSE-EDITORIAL-AUTHOR-SPEC.md`             |Spec — house / editorial author (**shipped**; see Settled).                                                                                                                           |
|`PRD-D-4-LATELY-MILESTONES-AND-PLUS2-REFRAME-SPEC.md`|Spec — Lately skill milestones + the +2 reframe.                                                                                                                                      |
|`PRD-D-5-QUESTION-QUALITY-FLOOR-VERIFICATION-SPEC.md`|Synthesis spec — question quality, difficulty floor, verification stack.                                                                                                              |
|`D-HOME-PACING-01.md`                                |Spec — Home pacing & budget model. **Superseded for Home by `D-HOME-DASHBOARD-MODEL-01` (2026-06-15)** — the budgeted-edition model is retired for Home; see Settled.|
|`D-HOME-DASHBOARD-MODEL-01.md`                       |Decision — **Home is a bounded 7-day dashboard** (rolling now − 7d over Zone 2; stated "Past 7 days" band; per-section honest empties; exhausted bundles hidden; cold-start window relaxation). Supersedes `D-HOME-PACING-01`; grounded in `_docs/HOME-DASHBOARD-AUDIT-01.md`. Authorizes the `B-HOME-WINDOW-02 … -COLDSTART-06` slate; see Settled.|
|`D-REFLECTION-COPY-01.md`                            |Copy spec — Weekly Reflection ceremony, warm-register words-before-pixels gate. **Built** via `B-REFLECTION-CEREMONY-01` (copy + hierarchy + Beat E selection); 🔁 re-audit after merge. See Settled.|
|`_docs/D-FEED-FRIEND-ACTIVITY-01.md`                 |Spec — “From Friends” chronological activity log. **Built (cut-1) and live** despite the “SKETCH/not yet wired” header in `src/lib/friend-activity.ts` — header is stale; see Settled.|

Execution scaffolding (kept separate, not product spec): `docs/build-prompts/`.

> **Note on missing referenced docs:** `Master_App_Instructions-v2.md` and `B-CATEGORY-AUTHORED-RECONCILE-01.md`
> are referenced elsewhere but **do not exist in this branch**. `CATEGORY-HIERARCHY-FINDINGS-01.md` exists but is
> itself stale (it claims the authored path “categorizes blind” — no longer true; see Settled §authored-reconcile).

## Settled decisions (don’t re-open without cause)

- **Directional follow is the primitive.** [built] Symmetric friendship replaced; “friend” = mutual follow. (`PRD-D-1` Decision 1.)
- **Authored-vs-curated provenance is honest.** [built] Forwarded LLM questions get `creatorId: null`, `source: 'curated_sent'`; credit never accrues to the forwarder.
- **Send difficulty travels with the question.** [built] The forwarded question keeps its own `difficultyEstimate`. (`PRD-D-0` §4.1.)
- **Broadcast rolls off after unfollow — won’t fix.** [built] Already-surfaced broadcasts are not retroactively purged on unfollow. (`PRD-D-0` §4.2.)
- **Question quality = floor + verification, shipped together.** [built] Full D1–D11 ledger locked in `PRD-D-5` §4; source strategy is open-web-plus-trust-layer, not a curated corpus. Two as-built deviations (`PRD-D-5` §11):
  - **Embedding provider = Voyage AI `voyage-3.5-lite` (1024-dim), gated on `VOYAGE_API_KEY`** — OFF until the key is provisioned; falls back to deterministic fact_key + Haiku + normalized-text guards.
  - **D9 attribution = universal profile links, sender-only, no friend/stranger gating** — safe because no surface shows a per-author avatar to strangers and the profile page is relationship-gated.
- **House author shipped.** [built] `source='house_authored'`, `pickHouseQuestions` (called from the live queue at `queue-orchestrator.ts:269`), `buildHouseSlot`, smoke script. The conformance audit’s §3.1 “UNBUILT” is **stale**.
- **House-origin questions never render peer copy (Invariant H-1).** [built, regression-tested] **Audit correction: this is closed, not open.** `'A friend'` cannot reach house questions — house questions are non-feed-eligible (`visibility.ts:64,78`) so the unguarded fallback is unreachable, and where house renders, author resolution is centralized in `resolveAuthorDisplay` (house→`'Joshing'`, ending in `assertNever`). Asserted by `questions-types.test.ts:51-54` and `GameplayChat.house.test.tsx:46`.
- **`/replay` and `/archive` are built but deliberately unlinked.** [built] Zero inbound links by design (`PRD-D-0` §2), not dead code. Open question is only whether to revive/delete; replay carries live grading code that must track the answer paths while orphaned. *(Caveat: `archive` still renders live thumbs buttons — see the `surfacePriorityScore` entry.)*
- **Home is a budgeted edition, not a log.** ⚠️ **SUPERSEDED FOR HOME by `D-HOME-DASHBOARD-MODEL-01` (2026-06-15)** — Home is now a bounded 7-day dashboard, not a budgeted edition (see the next entry). The mechanisms below remain accurate as the *as-built starting point* the dashboard slate edits: the served caps, the hero/panel/composer chrome, and the silent refill-from-queue all carry forward; what changes is the bounding instrument (window, not budget), the temporal/empty treatment (band label + per-section honest empties, §4/§9 reversed), and spent-card lifecycle (§Q4 reversed, exhausted bundles hidden). [built, cut-1] `D-HOME-PACING-01` ratified; the budget edition is live (`src/server/home/select-edition.ts`, `build-edition.ts`, rendered via `FeedList` with a `budget` prop). Caps: Direct 3 / Playables 4 / Texture 8; single `TodaysFiveCard` hero; one rotating panel; composer footer. Serve-and-overflow: direct → `/for-you`, playables → `/from-friends`, both back-navigable. **Refill-from-queue is silent and confirmed in code:** Home and subpages read the same queries; answering calls `router.refresh()` and Home re-windows top-N — no ghost states, no manual clear (`OverflowSubpageSync.test.tsx`, `FeedListBudget.test.tsx`). Temporal archive removed from the budgeted path (`groupItemsByRecency` runs only on the logged-out `!budget` branch). *Resolves the canon’s §9 item 6.* **Drift note:** `PersonActivityCard` / `groupActivityByFriend` are still imported in `FeedList.tsx` but inert on the budgeted home (`groupableFriendId` returns null for feed/milestone rows) — behavior is correct, but the symbols are present-but-defanged, not removed.
- **Home is a bounded 7-day dashboard.** [decided 2026-06-15; `-02`/`-03`/`-04`/`-05` built, `-06` pending] `D-HOME-DASHBOARD-MODEL-01` ratified; **supersedes `D-HOME-PACING-01`** (the budgeted-edition model is retired for Home). The bounding instrument is a **rolling now − 7-day window over Zone 2** (the ambient band: From Friends, Recent Activity, Shared Ground/convergence), not a budget — served caps stay (Direct 3 / Playables 4 / Texture 8; the audit's 5/5/6 cap change is **withdrawn**) but are no longer the primary bound. Zone 1 (directed "For You") is **unwindowed** — a sent question is a gift that persists until played/dismissed, pins above the band, and its `/for-you` overflow stays all-time. The band carries a single **"Past 7 days"** label stated once (per-section headings dropped/demoted); each Zone-2 section shows a **per-section honest empty state** when empty-in-window rather than being hidden or backfilled (reverses `D-HOME-PACING-01 §4`/`§9`). Exhausted From Friends bundles are **hidden once all questions are played** (shows "N of M" while answerable) — partial reversal of `D-FEED-FRIEND-ACTIVITY-01 §Q4`. **"View more" portals** (`/from-friends`, `/activities`) are bounded to the same 7 days; the standalone Activities tab (`/api/activities` → `getActivitiesForUser`, PRD §8.15) keeps its own 90-day cutoff. **Cold-start exception:** for a user with friend count ≤ 1, Zone 2 relaxes the window to the inviter's content and the band label is **suppressed** (it must not assert a false 7-day promise on an unbounded page); re-engages automatically at the 2nd friend. **Build state on this branch:** `B-HOME-WINDOW-02` (window + recent-or-nothing `boundTexture` + per-section empty signal), `B-HOME-OVERFLOW-WINDOW-03` (overflow portals), `B-HOME-BAND-LABEL-04` (band label + honest-empty rendering), and `B-HOME-EXHAUST-05` (exhausted bundles hidden per-viewer; remaining-count display) are merged; `B-HOME-COLDSTART-06` (window relaxation + label suppression via the `bandLabelVisible` seam) is pending. The "N of M questions" label shows the REMAINING (unplayed) count, in lockstep with the solid bundle triangles. Grounded in `_docs/HOME-DASHBOARD-AUDIT-01.md`.
- **“From Friends” is a chronological activity log.** [built, cut-1] Live: `/from-friends` → `getFriendActivity` → `deriveFriendActivity`, recency-ordered. Replaces the deep/breadth domain-grouped mastery summary (the old `deriveLatelyMilestones` still exists but has no production call site here). Context-routed burst grouping, held-singles, static cards, inline play (`PendingPlayablesList` → `InlineAnswerFlow`) all present. As-built sub-decisions: burst gap **45 min**; held-singles **pure re-derivation, no table** (solo release 5d, flush-to-mixed at 2 singles, 35d window); completed cards roll off at **30 days**, no history view *(⚠️ §Q4 "spent cards stay 30 days" is overridden for the fully-exhausted case by `D-HOME-DASHBOARD-MODEL-01 §3`, built in `B-HOME-EXHAUST-05`: a bundle the viewer has fully played is dropped in `build-stream.ts` on exhaustion, not on the 30-day timer — so the timer no longer governs home From Friends. There is no discrete roll-off job to remove; `deriveFriendActivity` is read-time, bounded by the scan window, so the timer is moot, not deleted)*; copy is **one shared lead set, not per-context** (context is computed but unused). ⚠️ **Open defect, not a decision:** the shipped lead copy still contains competition-register phrases (“on a roll,” “on a streak,” “on a tear,” “killing it” in `activity-stream.ts:858-871`) that the locked discovery-register rule prohibits. Tracked in Open.
- **Authored-question categorization reconciles against existing domains.** [built, flag-off] **Audit caveat: ships behind `RECONCILE_AUTHORED_DOMAINS`, flag-OFF (shadow-log only) — the fold is computed but not yet adopted.** When on, the authored path reconciles using **both** `convergeDomain` (pg_trgm `similarity()`) and `reconcileProposedDomain` (Haiku), sequential/short-circuiting; the fold is **silent** (no client-facing confirm; `category_overridden` appears in zero `.tsx`). Reuses the shared helpers, not a reimplementation. Decided against and confirmed out: no middle tier, no parent/child edge or roll-up, no schema change. Generated/onboarding paths untouched. *(The `CATEGORY-HIERARCHY-FINDINGS-01.md` “categorizes blind” claim is stale.)*
- **Directed-question card: gold-sans eyebrow on the plain bordered tile.** [built] The triangle-mat frame on directly-sent cards is retired (`DirectSentCard` uses `variant="bordered"`), and direct sends are marked out by an eyebrow rather than chrome: gold sans **“Sent directly to you”** in `--accent-gold` (#d9a82e), `font-semibold` (`DirectSentCard.tsx:47-49`). There is **no left edge-bar** — `SparkleEnvelope` never threads `accentColor`, so `FeedCardShell.accentBar` stays null. Broadcast (“added a question”) cards stay the plain default with no eyebrow. **This is canon** (`D-FEED-DIRECTED-CARD-RECONCILE-01`, resolved Option A 2026-06-13): the originally-specced amber/`HILITE` left edge-bar + Courier “SENT TO YOU” eyebrow were provisional paper ideas that did not ship and are not being built — the gold-sans eyebrow reads as warm-and-distinct enough on its own, and keeping `HILITE` Lately-only avoids extending that token to a second surface. The Friend Play edge-bar sub-question is moot and struck — no Friend Play card renders an edge-bar on the feed surface at all.
- **Color system: grading and category colors are to be separated by construction.** [decided, NOT built] **Audit correction — the decision stands but the build never landed; the collision is still live in the default.** Decided: WRONG moves to a true red out of the terracotta family; category color = top-level domain with leaves inheriting; the mastery triangle encodes completion (fill) + is decorative-hashed, never correctness (the only correctness-colored shape is `GeometricProgress` dots, which is correct). **As-built reality:** the default `:root` still holds the colliding values — `--game-wrong-strong:#c33d14` (`globals.css:109`) vs `--cat-literature:#c0392b` (`:119`). The de-collided true-red (`#c1121f`) lives only inside an inert `:root[data-palette="proposed"]` block that **nothing in live code activates**; `PaletteToggle` was repurposed to a card-bg cycler and is still mounted marked “TESTING ONLY — remove before shipping.” `B-VISUAL-PALETTE-PROMOTE-01` / `LITERAL-TO-TOKEN-01` do **not** exist in the repo. Leaf-inheritance (`getPortraitDomainColor`) and the triangle rule *are* live. **This is the top remediation item** — see follow-up B-prompt below.
- **`surfacePriorityScore` is a dead column — for surfacing.** [built, blocked-on-thumbs-UI] Thumbs feedback does not affect feed ordering: the feed orders strictly by `desc(sourceEventAt), desc(id)`; nothing reads the column for ranking, and it’s excluded from the question view DTO. **Audit correction: it cannot simply be dropped yet** — `QuestionRatingButtons` (still POSTing to it) remains live on **two surfaces**, `games/[id]/summary/page.tsx:371` (a linked surface) and `archive/page.tsx:414` (unlinked). Daily summary was migrated to a Heart writing `questionFeedback` only. Removal sequence: rewire/remove those two button surfaces → drop the 4 `ratings.ts` writes → drop the column. (Replaces the former Open “Thumbs-up → surface priority.”)
- **The 15-item “design consistency audit” is reviewed and closed — zero net-new findings.** [decision, not code] An external generic consistency audit (15 items + a sweeping token-system + 30-day polish plan) was dispositioned in `D-CONSISTENCY-AUDIT-DISPOSITION-01.md`: 0 net-new actionable findings; the only real thread (hardcoded literals → tokens) is already owned by `B-VISUAL-TOKEN-BUDGET-01`; the rest are contradicted by canon (category colors are identity signals, blue is CTA-only, flat letterpress is intentional, card tiers/bespoke primitives deferred by decision), invented, or generic boilerplate. **Do not convert it into tickets.** Real visual-consistency work must come from a pass against live `globals.css` + the component tree, not that audit.
- **Zod-validation convention applies to structured request bodies.** [built] JSON request-body handlers validate with Zod (audit finding E, 2026-06-04). Four documented carve-outs keep their existing validators (query-param routes already coerced/clamped; `POST /api/questions` via centralized unit-tested `readCreateQuestionPayload`; `propose-interests` and `friend-invitations` emit distinct field-specific error codes a single schema would flatten; `PATCH /api/declared-interests` drops-not-rejects invalid items). Converting later is safe-but-optional; tests catch drift.
- **Weekly Reflection — warm-register build + Beat E discovery selection.** [built] 🔁 **re-audit after merge.** `B-REFLECTION-CEREMONY-01` landed the D-REFLECTION-COPY-01 copy across all five beats (A–E) in `src/app/ceremony/[ceremonyId]/page.tsx`, killed the repeating circle/title/status triad (one strong slide per beat; lead large, rest tucked behind tappable “+{n} more”), and built the real Beat E. **Beat E selection rule resolved:** missed/expired canonical answers (`masteryEvents.answerState='incorrect'`), ranked **friend-co-miss → most-recent → hash-stable (djb2)** via the pure `selectDiscoveries` in `compute-beats.ts` (co-miss mirrors `getLatelyConvergences`, flipped to incorrect), curated to 3. **Scope decision:** curated cards are **canonical-only** (bot daily slots share no question id, so co-miss is undefined and there's no canonical text to render); all discoveries incl. bot slots live behind the `View all discoveries →` deep-link to `/replay`. Copy is honest to the implemented signal (no softening needed). *Flag-only carry-overs:* payload field/type still named `beat6`/`Beat6Learned` (semantics now Beat E); the partial `ceremony`/`biweekly` rename (table, route, activity type) is unchanged. (Supersedes the former Open “Beat E discovery selection rule.”)

- **Account deletion honors the author's exit without confiscating third parties' proven territory.** `[built]` 🔁 **re-audit after merge.** Ratified 2026-06-15 (`D-ACCOUNT-DELETION-TERRITORY-01`; FK inventory + conformance audit in `_docs/D-ACCOUNT-DELETION-TERRITORY-01-FK-AUDIT.md`). The four assets bundled in "delete" resolve separately: **(#1)** retained answerers' mastery events + points earned off the departed author's questions are **PRESERVED** (proven territory is the answerer's evidence, not contingent on author presence); **(#2)** the authored `Question` is **TOMBSTONED** — `creator_id` nulled, `source='house_authored'`, content/`asked_count`/`correct_count` retained so difficulty + proven-territory math + §8.27 retroactive reclassification stay computable; **(#3)** the windowed §16.16 author-credit (`MASTERY_EVENTS.source_type='author_credit'`, `user_id`=author) is **REMOVED** with the author; **(#4)** the departed author's own declared+proven territory, portrait, profile are **HARD-DELETED**. Ratified forks: **A1 tombstone** (not A2 snapshot — snapshot freezes answerer portraits and diverges from §8.27); **tombstone identity = reuse `house_authored` + a new `author_deleted` boolean** on `Question` to distinguish it from genuine house content (never renders as a person, Invariant H-1); **C1** preserve convergence/Joshing-game facts for the retained user but degrade attribution to the anonymized source (C2 remove acceptable for un-surfaced events); **D re-source to house** — repoint `FeedItem.sourceUserId` to the house sentinel and render via the tombstoned question (mirrors the existing `activityItems.actorUserId='set null'` precedent), do not leave dangling. Decision **B** (author content with no retained dependency, incl. nobody banked it) hard-deletes with the account; a friend's bank row makes it a retained dependency → falls under A1. Decision **E** (departed user's own answerer credit) hard-deletes with #4; no retained aggregate reads it. **Why no `ON DELETE CASCADE`:** the territory-critical FKs (`MASTERY_EVENTS.user_id`/`question_id`, `playerMastery.user_id`, `questions.creator_id`, `FeedItem.sourceUserId`/`questionId`) are all live-RESTRICT (no `onDelete`), so a raw `User` delete is *blocked*, not silently destructive — the deletion path must be an **application-level routine** (tombstone + selective delete), never new cascades. **Build state:** nothing built. Blockers before the endpoint: add `Question.author_deleted`; write the deletion routine handling every RESTRICT FK in the audit; 🔁 re-audit live schema vs. this decision; then the pre-launch "account deletion behavior" checklist item can be checked. Sub-questions resolved in audit: `SharePortraitCard` carries no author names (no anonymization needed); answerer tiers are independent of author-credit (`scoring.ts`); tombstoned questions stay in the difficulty-calibration denominator. **As built (2026-06-15):** migration `0080_question_author_deleted` adds `Question.author_deleted` (+ partial index + instrumentation guard); `deleteUserAccount` (`src/server/db/queries/account.ts`) was rewritten from the hard-cascade — it now partitions the author's questions into tombstone vs. orphan, deletes ONLY the departed user's own `MASTERY_EVENTS` (never by `question_id`, which was the confiscation bug), nulls `answered_by_user_id` for retained convergence rows (C), and re-sources via the tombstone (`creator_id` NULL + `source='house_authored'` → `resolveQuestionAuthor` renders house, H-1 intact). **Two as-built notes:** Decision D's "re-source to house" is realized as **strip the actor cards** for `FeedItem.sourceUserId` (a real `users.id='house'` is forbidden by H-1; the question half still re-sources to house via the tombstone) — ratified 2026-06-15; and `ContentReport` (reporter/target RESTRICT, previously unhandled) is now **handled** — its `one_target` CHECK forbids nulling a vanishing target, so reports owned by the departed user or targeting a hard-deleted orphan/generated question are deleted, while reports targeting a surviving tombstone keep their link (moderation record preserved). No live integration test in CI (the query suite is DB-mocked; the raw-SQL tx isn't exercisable there) — but a runnable conformance script ships: `npm run verify:account-deletion` (`scripts/account-deletion-territory.verify.ts`) seeds the full territory scenario against a real DB, deletes the author, asserts #1–#4/B/C/D + ContentReport, and self-cleans. Run it against a real database at the 🔁 re-audit before launch.
- **Color system: de-collided palette is the default.** `[built]` WRONG is a true red out of the terracotta family (`--game-wrong-strong: #c1121f`), de-collided from the brand/link orange and from Literature's category mark (which moved to bordeaux `--cat-literature: #7d2c3f`); Language moved off the CORRECT green to teal (`--cat-language: #2e6e7e`). These were formerly inert behind `:root[data-palette="proposed"]`; they are now the base `:root` values in `src/app/globals.css` and the `data-palette="proposed"` block is deleted. `SharePortraitCard.tsx`'s hardcoded html2canvas scale is re-synced value-for-value (kept in lockstep by comment). Category color = top-level domain with leaf inheritance via `getPortraitDomainColor` (unchanged). (`D-DECISIONS-CONFORMANCE-01` §5; built 2026-06-13 in `B-VISUAL-PALETTE-PROMOTE-01`.)

## Open — pick these up

- **Land the palette promotion for real.** [highest priority] The grading/category collision is **live in the default** (`#c33d14` vs `#c0392b`); the fix is inert behind `data-palette="proposed"`. Promote proposed→default, retire the still-mounted `PaletteToggle`, **and** re-sync `SharePortraitCard.tsx`‘s hardcoded hex (html2canvas can’t read CSS vars — it currently mirrors the OLD scale and would drift the instant the palette flips) in the same prompt. Minor tail: `DomainCircle.tsx` still hardcodes `#d4cfc7` / `#c8c0b0`. See B-prompt `B-VISUAL-PALETTE-PROMOTE-01`.
- **From Friends copy violates the discovery-register rule.** Remove “on a roll / on a streak / on a tear / killing it” from `activity-stream.ts:858-871`. This is a defect against a locked decision, not an open product question — but it needs a prompt.
- **Adopt or hold `RECONCILE_AUTHORED_DOMAINS`.** The authored reconcile is built but flag-off (shadow-log only). Decide when to flip after reviewing the shadow-log duplication numbers.
- ~~**Option B: should the aside amplify a human’s `creatorNote` when one exists?**~~ **Resolved (2026-06-15) — unified.** The aside and the creator note are now ONE note (`src/components/CreatorNote.tsx`), and treatment is chosen by **provenance, never answer-state**: a human author speaks in a bold inverted INK block ("Between you and {Name}"); house/LLM render as the quiet bronze editorial aside ("Between us!"). A human question never carries a machine-generated aside — `generateInsideJoke` was removed from the authored create path (`POST /api/questions`); the daily/LLM + house generation paths are unchanged. (`B-CREATORNOTE-EDITORIAL-ASIDE-01`; supersedes `PRD-D-0` §5.)
- **Niche-match production default.** Ships `false`, test-phase `true`; production default open, revisit after the test. (`PRD-D-2`.)
- **Feed verb `'wrote this'` on broadcast.** The `authored_shared` branch claims authorship unconditionally; resolve. (Conformance audit §3.2.)
- ~~**Build account-deletion behavior.**~~ **Built (2026-06-15)** — `0080_question_author_deleted` + the rewritten `deleteUserAccount` (tombstone partition, retained-mastery preservation, C-degrade, D actor-strip). See the Settled entry above. **Remaining before the pre-launch checkbox:** run the shipped conformance script `npm run verify:account-deletion` against a *live* database (the rewrite is typecheck/lint/mock-test verified, but the raw-SQL transaction isn't exercised by the DB-mocked query suite). The script seeds the territory scenario, deletes the author, asserts the ratified behavior, and self-cleans.

## Known-stale source claims

Corrected at the source when next editing those files:

- **Conformance audit §3.1 — “house author UNBUILT.”** Stale; house author is live.
- **`D-HOME-PACING-01.md` header — “not yet sequenced into build prompts.”** Stale; built cut-1 and live.
- **`src/lib/friend-activity.ts:1-6` — “SKETCH / not yet wired in.”** Stale; wired and live.
- **`CATEGORY-HIERARCHY-FINDINGS-01.md` — authored path “categorizes blind.”** Stale; reconcile path exists.
- **`PRODUCT-CANON.md` §9 item 6 — Home-pacing-vs-tiers sequencing “unresolved.”** Stale; sequenced and built.
