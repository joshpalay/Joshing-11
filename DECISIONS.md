# Decisions & Docs Index

A short index of the durable restructure (D-1 / D-2 / D-3 / D-4 / D-5) documentation, and — critically — a
pointer to what is **settled** vs. **still open**, so the next reader doesn’t re-litigate closed
decisions or miss the live thread.

The `PRD-D-*` series **is the current product canon** (the “v12 line”) and supersedes the 10.25/v11.x PRDs.
The older specs are archived under `_docs/archive/` for history: `PRD-v11.2.md` (the last v11 diff, with
v11.3 / v11.4 / v11.5 folded in place — there is no standalone v11.3+ file), `PRD-v11.1.md`, `PRD11.md`,
and `Joshing_PRD_v10_25 (1).md`.

**A “Settled” decision is one that has been *made*, regardless of build state.** Each settled entry carries
a build tag so a made-but-unshipped decision is never mistaken for live behavior:

- `[built]` — live in the default code path.
- `[built, flag-off]` — code merged but gated behind a flag; computed, not yet adopted.
- `[decided, NOT built]` — the call is made; the code does not yet reflect it. **Live behavior still differs.**
- `[built, specced≠as-built]` — something shipped, but it diverges from the decision as written; reconcile pending.

Decision state and build state are tracked independently. The build tags below were verified against live code
by `D-DECISIONS-CONFORMANCE-01` (2026-06-13); where the audit refuted an earlier assumption, the entry says so.

Last updated: 2026-07-08 (D-SUPPLY-LADDER-UNIFY-01 **RATIFIED — A2 + D2 + E2** (Josh, during the question/gameplay/cost health review): rung-5 content-exhaustion = effective depth < threshold AND last refill recovered zero new facts (A2, additive to the difficulty-ceiling stamp); graduation freezes the leaf's *refill demand* and retargets the parent while keeping the leaf's territory (D2, consistent with `D-AREA-EXPANSION-01` §9 R4/E1); flips stage refill → soak ≥1wk → guard → soak ≥1wk → rung-5 refinement (E2). B2/C1 were already live canon. Ratification settles definitions only — flip 1 stays PAUSED per `B-SUPPLY-REFILL-EFFORT-REPORT.md`; the E2 clock starts when refill resumes after its finite-set re-scope. Prior: 2026-06-30 — D-SUPPLY-LADDER-UNIFY-01 [DRAFT] — unifies the three narrow-domain supply mechanisms into one ordered ladder: grounded **refill** (`RETRIEVAL_GROUNDING_ENABLED`, off) → narrow-KB **guard** (`NARROW_KB_GUARD_ENABLED`, off) → honest **broaden-borrow** (on) → **graduation** (Area Expansion). Flip order is canon: refill before guard, always (S5). A live-code reconciliation corrected the original draft: `D-AREA-EXPANSION-01` is **settled + §9-amended + largely built**, so rung 5 is **not** greenfield — the expansion trigger (`recalibrateDomainDifficultyToSupply` → `expansionEligibleSince`), surface (daily-summary `ExpandDomainOfferCard`), and write (`addDeclaredInterest` chokepoint, **not** `openKBDomain`) already ship; this doc only adds a content-exhaustion gate (decision A) and a leaf-refill-freeze (decision D). The §4 effective-depth seam spans a table boundary (`needs_review`/`deletedAt` live on `Question`, but `getDurablePoolDepthForDomains` counts `generatedQuestions`). **§4 build correction (B-SUPPLY-REFILL-FLIP-01, 2026-06-30):** the machine-pool depth metric that refill + the guard actually read (`getDurablePoolDepthForDomains` / `getThinActiveDomains`) is **already correct and unchanged by flip 1** — it counts `count(distinct fact_key)` over `generatedQuestions` where `is_duplicate = false ∧ fact_key is not null`; `generatedQuestions` has **no `deletedAt`** (machine rows are flagged, never deleted — `schema.ts:710-713`) and every `suppressed_by` loser is a strict subset of `is_duplicate = true` (`markPoolDuplicate` sets both, `pool.ts:286-302`), so no not-live machine row is counted as supply. §4's "exclude `needs_review`" tripwire concerns **`Question`** rows the metric never counts (the machine pool has no `publicStatus`), so it is **N/A here — not violated, nothing to filter**. Whether a human-side `needs_review` demote of a *promoted* machine fact should retroactively lower machine-pool supply (§4 options i/ii/iii / §9 cross-boundary helper) is **deferred to the expansion-refinement build, not landed by this flip** (per its DO-NOT). **Verification finding (B-SUPPLY-REFILL-FLIP-01, 2026-06-30 — flip BLOCKED):** the prompt's "mostly a flag flip" premise is wrong. A real run persisted 4 corroborated facts and the quality gates are healthy (57% of completions persist; only correct corroboration drops, 0 quality/ask drops) — but the refill is **sequential** and **65% of thin domains time out at the 120s per-call limit**, so a 300s cron drains only ~2–3 domains and the slow domains never deepen. A new prerequisite **`B-SUPPLY-REFILL-THROUGHPUT-01`** (bounded-concurrency domain processing, cap ~4 vs the `max:5` pool, + per-wave budget guard) must land before the flip. Also: refill's **web-search spend is not in `LlmUsageEvent`** (token-only ledger). **⏸️ RESOLVED → PAUSED (2026-07-01):** throughput built + merged (PRs #1337/#1341/#1342), the prod flip was attempted and reverted (0 persists / 8 timeouts / 504), and diagnostics proved the blocker is **variable, un-config-tunable per-call latency** (~25s → >220s for the same grounded call within an hour) — neither web search nor concurrency. The only structural fix is the **async Batch API** (no 300s ceiling), gated on `web_search`-in-Batch feasibility; at 18-user scale that re-architecture is deferred. Grounding stays `false`; path documented in **`B-SUPPLY-REFILL-EFFORT-REPORT.md`**. Full detail: `B-SUPPLY-REFILL-FLIP-01-FINDINGS.md`. `RETRIEVAL_GROUNDING_ENABLED` stays `false`; `NARROW_KB_GUARD_ENABLED` untouched (S5). Open for ratification: A (exhaustion def, rec A2), D (leaf refill fate, rec D2), E (flip staging, rec E2); B/C closed by live canon. See `D-SUPPLY-LADDER-UNIFY-01.md`. Prior: 2026-06-26 — D-NARROW-KB-FABRICATION-01 — niche-fiction fabrication on a narrow declared-interest KB ("Spy School Books 1-6": an invented "Academy of Evil" cover name, a real university named as a fictional school's front) is a GROUNDING problem, not a gate problem: an A/B over the exact questions showed neither Sonnet nor Opus, under any prompt, catches fabricated niche canon — extending D-LLM-PROVIDER-AB-AND-GATE-TIER-01 past tier. Fix at generation with two flag-off levers: (A) a narrow-KB exhaustion guard `NARROW_KB_GUARD_ENABLED` that suppresses fresh ungrounded generation for declared-interest domains thinner than grounding's `RETRIEVAL_POOL_DEPTH_THRESHOLD` and backfills from other domains; (B) the already-built, dormant B3 retrieval-grounded refill, flipped in the same change (operator-gated, billable). Both `[built, flag-off]`. Plus a now-FIXED operational finding: the `daily-assignments` queue-build cron was under-covering (a ~20-min cold-boot `migrate()` hang blew the 300s budget; ~6/17 users covered) → on-demand slow loads; fixed by bounding the cold-boot DB connect + `migrate()` timeout, a soft per-run deadline with a clean result, and idempotent catch-up cron passes (17:05/17:30/18:00 UTC). See Settled. Prior: D-RECOVERY-SCORING-UNIFY-01 — answer-point scoring is centralized in one function `canonicalPointsForAnswer` across all five answer routes; the multiply path is canonical (A1), all routes route through the helper (B1), a per-tier invariant test pins the table column to `round(first_correct × RECOVERY_STATE_WEIGHT)` (C1), and catch-up recovery is preserved at 25% of full base — not the stacked 6.25% the helper originally computed (D1, surfaced during build). Player-visible points byte-identical; `POINTS_PER_CORRECT` retired. See Settled. Prior: D-LOADING-MOMENT-SURFACE-01 — design doc for the Loading Moment, a rotating wait-time surface showing one warm, true artifact from the player's portrait/social graph; never a score or ranking. Card taxonomy + fallback ladder + read-only/cached-only constraints (C-1…C-7) defined; all five drafting sub-questions now resolved (long-waits-only scope; wrong→right-only card 3; overlap weighted below discovery; client-only anti-repeat; correct-only/lowest-priority card 4). Precedes `B-LOADING-MOMENT-*`, does not authorize a build. Prior same-day: D-LLM-PROVIDER-AB-AND-GATE-TIER-01 — the Anthropic↔OpenAI provider A/B switch is test instrumentation defaulting to Anthropic; a 2026-06 audit showed internally-inconsistent / false-premise questions are a model-TIER problem on the factual gate, NOT a provider or prompt problem, so the factual gate defaults to Sonnet via `FACTUAL_GATE_MODEL`. See Settled.)

## Durable docs

|Doc                                                  |What it is                                                                                                                                                                            |
|-----------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|`PRD-D-0-PRODUCT-DIRECTION-AND-DECISIONS.md`         |Canonical “what the product is and why”: target capabilities, deferred items, open hypotheses, **amended decisions**.                                                                 |
|`PRODUCT-CANON.md`                                   |Designer-facing product canon. (Stale in §8/§9 as of 2026-06-13 — see canon-edits note at the bottom.)                                                                                |
|`PRD-D-1-FEED-DAILY-RESTRUCTURE-SPEC.md`             |Spec — directional follow + Feed/Daily split.                                                                                                                                         |
|`PRD-D-2-NICHE-MATCH-DISCOVERY-SPEC.md`              |Spec — niche-match discovery engine.                                                                                                                                                  |
|`PRD-D-3-HOUSE-EDITORIAL-AUTHOR-SPEC.md`             |Spec — house / editorial author (**shipped**; see Settled).                                                                                                                           |
|`PRD-D-4-LATELY-MILESTONES-AND-PLUS2-REFRAME-SPEC.md`|Spec — Lately skill milestones + the +2 reframe.                                                                                                                                      |
|`PRD-D-5-QUESTION-QUALITY-FLOOR-VERIFICATION-SPEC.md`|Synthesis spec — question quality, difficulty floor, verification stack.                                                                                                              |
|`D-HOME-PACING-01.md`                                |Spec — Home pacing & budget model. **Superseded for Home by `D-HOME-DASHBOARD-MODEL-01` (2026-06-15)** — the budgeted-edition model is retired for Home; see Settled.|
|`D-HOME-DASHBOARD-MODEL-01.md`                       |Decision — **Home is a bounded 7-day dashboard** (rolling now − 7d over Zone 2; stated "Past 7 days" band; per-section honest empties; exhausted bundles hidden; cold-start window relaxation). Supersedes `D-HOME-PACING-01`; grounded in `_docs/HOME-DASHBOARD-AUDIT-01.md`. Authorizes the `B-HOME-WINDOW-02 … -COLDSTART-06` slate; see Settled.|
|`D-REFLECTION-COPY-01.md`                            |Copy spec — Weekly Reflection ceremony, warm-register words-before-pixels gate. **Built** via `B-REFLECTION-CEREMONY-01` (copy + hierarchy + Beat E selection); 🔁 re-audit after merge. See Settled.|
|`_docs/D-FEED-FRIEND-ACTIVITY-01.md`                 |Spec — “From Friends” chronological activity log. **Built (cut-1) and live** despite the “SKETCH/not yet wired” header in `src/lib/friend-activity.ts` — header is stale; see Settled.|
|`D-LLM-PROVIDER-AB-AND-GATE-TIER-01.md`              |Decision + findings — the Anthropic↔OpenAI provider A/B switch (test instrumentation, default Anthropic) and the audit showing internally-inconsistent / false-premise questions are a model-**tier** problem on the factual gate, not a provider or prompt problem → gate defaults to Sonnet via `FACTUAL_GATE_MODEL`. **2026-06-26 addendum:** a precision/variance audit corroborated the tier finding (Haiku over-flags under a loose prompt; no unique Sonnet catches) and pulled 4 false-premise + 1 duplicate question from the live pool. See Settled.|
|`D-NARROW-KB-FABRICATION-01.md`                      |Decision + findings — niche-fiction fabrication on a narrow declared-interest KB is a **grounding** problem, not a gate problem (an A/B showed neither Sonnet nor Opus, under any prompt, catches fabricated niche canon — extends the tier finding). Two flag-off levers: a narrow-KB exhaustion guard (`NARROW_KB_GUARD_ENABLED`) and the dormant B3 retrieval-grounded refill, co-flipped. Plus an OPEN cron under-coverage finding (cold-boot `migrate()` hang). See Settled.|
|`D-SUPPLY-LADDER-UNIFY-01.md`                        |**RATIFIED 2026-07-08 — A2 + D2 + E2 (B/C by live canon).** Decision record — unifies narrow-domain supply into one ordered ladder (grounded refill → narrow-KB guard → broaden-borrow → Area Expansion graduation), flip order canon (refill before guard, S5). Live-code-reconciled: `D-AREA-EXPANSION-01` is settled+amended+largely built, so rung 5 already ships (trigger/surface/write) and this doc only adds a content-exhaustion gate + leaf-refill-freeze; the §4 effective-depth seam spans a `generatedQuestions`/`Question` table boundary. **Build prompts may be written in §8 order — but flip 1 (refill) stays PAUSED pending its finite-set re-scope.**|
|`D-LOADING-MOMENT-SURFACE-01.md`                     |**Sub-questions resolved 2026-06-25; copy strings + per-card budgets TBD in build.** Design doc — the **Loading Moment**: a rotating wait-time surface that shows one warm, true artifact from the player's portrait/social graph (never a score/rank). Defines the six-card taxonomy, copy register, fallback ladder, and the read-only/cached-only/truth-gate constraints (C-1…C-7) a build must honor. All five drafting sub-questions resolved (long-waits-only scope; wrong→right-only card 3; overlap below discovery; client-only rotation; correct-only/lowest-priority card 4). Precedes `B-LOADING-MOMENT-*`; **does not authorize a build.**|
|`D-KNOWLEDGE-MAP-USABILITY-01.md`                    |**RATIFIED + BUILT (2026-07-03)** — bubble-map usability: diagnosed the root-view label soup, gray-hue fallback, unconfirmed instant ghost-adopt, undiscoverable traversal, and the orphaned `/knowledge/[domain]` detail page; the `B-KNOWLEDGE-MAP-USABILITY-01 P1–P7` slate is built. As-built: one-label-layer rendering with a text halo + "+"-prefixed always-labeled ghosts (`KnowledgeBubbleMap`), stable hue fallback for unmapped broad categories (`hueForBroadCategory`), tap-=-select action card (`KnowledgeNodeCard`) with **confirmed** add → "Quiz me now", explicit ↑ up control, parent gap view from the new `KnowledgeTreeNode.progress` payload ("N of M · pts/threshold" + Fill-this-out roster), Add-an-area sheet on the map chrome (`AddAreaModal` wrapping the shared `AddTopicField`), hierarchical tappable list view, `KNOWLEDGE_MAP_PAGE` documented in `.env.example`. No schema change, no new mastery math. §5 answers recorded in the doc header (confirm-sheet; first-tap-selects; per-item add — the frequency endpoint has no cap; grow rim kept; flat page stays the flag-off fallback).|
|`D-MASTERY-FINEST-NODE-01.md`                        |**RATIFIED (2026-07-08).** Decision — over a **contained** hierarchy (substantive edges, per `D-KNOWLEDGE-TAXONOMY-MODEL-01`), finite completable sets force **fact-at-finest-node accounting**: a question credits its finest node (the play) and rolls up to ancestors (the genre), so a Lear fact answered under a "Shakespeare Tragedies" subscription still credits King Lear — otherwise a coarse subscription burns a fine set's supply and leaves it un-masterable. Chosen mechanism = **generate-at-parent, tag-at-finest (option a)**; serving (`pickBankSource`) is explicitly UNCHANGED. Cashes in the built-but-dark `KNOWLEDGE_MASTERY_V2` (`rollUpCredit`/`recomputeMastery`); the finest-grain signal (`subject_entity`) is already on every row. Phased P1–P4; latent at ~6 players. Completes `D-SUPPLY-FINITE-SET-01`.|
|`D-NEARNESS-LADDER-HYBRID-01.md`                     |**SETTLED (2026-07-01, config A2+B2+C2+D2+E1).** Decision — a **hybrid near-ness ladder** for the supply-ladder's broaden-borrow + expansion rungs (sibling of the PAUSED rung-1 grounded refill). Global **rungs** (sibling/cousin/parent/grandparent) computed once per domain by a lazy **Haiku** call (curated-first) into a new **`domain_relations`** table, on the shared `poolDepthThreshold` boundary; a free **per-player overlay** (territory ∪ answer-history ∪ declared) marks which rungs are "held". Supply borrows only from **held** rungs via the existing `selectOverflowParents` seam; unheld near rungs become opt-in **expansion offers** (never silent borrows). **No mastery roll-up** (§4 closed). B and D pressure-tested vs live code. **Authorizes the `B-NEARNESS-LADDER-*` build.**|

Execution scaffolding (kept separate, not product spec): `docs/build-prompts/`.

> **Note on missing referenced docs:** `Master_App_Instructions-v2.md` and `B-CATEGORY-AUTHORED-RECONCILE-01.md`
> are referenced elsewhere but **do not exist in this branch**. `CATEGORY-HIERARCHY-FINDINGS-01.md` exists but is
> itself stale (it claims the authored path “categorizes blind” — no longer true; see Settled §authored-reconcile).

## Settled decisions (don’t re-open without cause)

- **The supply ladder's open decisions are ratified: A2 + D2 + E2.** [decided 2026-07-08; no build authorized yet] Rung-5 content-exhaustion = effective depth < threshold **AND** the last refill run recovered zero new corroborated facts (A2 — additive to the existing `servedDifficulty === 'specialist'` stamp at `adaptive-difficulty.ts:540`, so a domain refill can still deepen is never prematurely graduated). Graduation **freezes the leaf's refill demand** and retargets the parent (D2 — consistent with `D-AREA-EXPANSION-01` §9 R4; the leaf's territory is kept per E1, never confiscated). Flips stage refill → soak ≥1wk → guard → soak ≥1wk → rung-5 refinement (E2; S5 refill-before-guard already canon). B2/C1 were live canon already. Ratification settles the *definitions* — flip 1 (refill) stays **PAUSED** per `B-SUPPLY-REFILL-EFFORT-REPORT.md`, and the E2 staging clock starts when refill resumes after its `D-SUPPLY-FINITE-SET-01` re-scope. Full record: `D-SUPPLY-LADDER-UNIFY-01.md` §5–§8.
- **Over a contained hierarchy, mastery is accounted at the FINEST node — not the subscription label.** [decided, NOT built — `KNOWLEDGE_MASTERY_V2` off] For **substantive** edges (every child fact is a valid parent fact, e.g. Hamlet → Shakespeare Tragedies), finite completable sets + cross-surface fact-dedup make separate parent/child ledgers **incoherent**: 12 Lear facts answered under a "Shakespeare Tragedies" subscription burn 40% of King Lear's ~30-fact set but (under separate-pool accounting) credit only Tragedies, leaving King Lear un-masterable (max 18/30). Fix = credit each question at its finest node and **roll up** to ancestors (`rollUpCredit` + `MASTERY_EVENTS`-by-`question_id` + `recomputeMastery`). Chosen mechanism = **generate-at-parent, tag-at-finest (option a)**; **serving stays flat** (`pickBankSource` unchanged). The finest-grain signal (`subject_entity`) is already emitted per row (`generate-questions.ts:247`); the supply-depth counter already aggregates over `substantiveDescendants`. Independent pools survive **only** for *collection* edges (Hamlet → Branagh's Films). **P1 (write-path) DEFERRED after a 2026-07-08 prod measurement:** it would re-file only ~16/954 rows because `subject_entity` is character-level ("the Fool") while nodes are work-level ("King Lear") — only ~4% align; the real prerequisite is a work-level signal, and the stranded-mastery case is still latent (67% of rows already file at leaves). Full record + phased P1–P4 + worked example + measurement addendum: `D-MASTERY-FINEST-NODE-01.md`.
- **LLM question-quality misses are a model-TIER problem on the factual gate — not a provider or prompt problem.** [built: A/B switch + readout; decided, PRs open: gate→Sonnet #1221, OpenAI flagship→gpt-4.1 #1219] A 2026-06 audit (`scripts/audit-gate-compare.mjs`, n=80) showed sharpening the gate prompt at Haiku tier did nothing (+0/+1), while Haiku→Sonnet caught the false premises (≈ Opus's lift at a fraction of cost). The original "ChatGPT (GPT-5.5) caught it" was a *tier* signal, not Anthropic-vs-OpenAI. So: the factual gate (`findFactualFailures`) defaults to **Sonnet**, overridable via `FACTUAL_GATE_MODEL`; the Anthropic↔OpenAI provider A/B switch is **test instrumentation** (default Anthropic) and is *not* the lever for question quality (the gate isn't even one of its four switchable surfaces). OpenAI flagship default = `gpt-4.1`. Full record + open threads (quality gate still Haiku; provider quality A/B at equal tier still unmeasured): `D-LLM-PROVIDER-AB-AND-GATE-TIER-01.md`. **2026-06-26 follow-up** (`scripts/audit-gate-variance.ts`, n=40, Haiku-ensemble vs Sonnet) corroborated this — Haiku *over*-flags under a loose prompt with **no** unique Sonnet catches, so an ensemble/cascade is not a Sonnet substitute — and pulled 4 false-premise + 1 duplicate question from the live pool.
- **Directional follow is the primitive.** [built] Symmetric friendship replaced; “friend” = mutual follow. (`PRD-D-1` Decision 1.)
- **Authored-vs-curated provenance is honest.** [built] Forwarded LLM questions get `creatorId: null`, `source: 'curated_sent'`; credit never accrues to the forwarder.
- **Send difficulty travels with the question.** [built] The forwarded question keeps its own `difficultyEstimate`. (`PRD-D-0` §4.1.)
- **Broadcast rolls off after unfollow — won’t fix.** [built] Already-surfaced broadcasts are not retroactively purged on unfollow. (`PRD-D-0` §4.2.)
- **Question quality = floor + verification, shipped together.** [built] Full D1–D11 ledger locked in `PRD-D-5` §4; source strategy is open-web-plus-trust-layer, not a curated corpus. Two as-built deviations (`PRD-D-5` §11):
  - **Embedding provider = Voyage AI `voyage-3.5-lite` (1024-dim), gated on `VOYAGE_API_KEY`** — OFF until the key is provisioned; falls back to deterministic fact_key + Haiku + normalized-text guards.
  - **D9 attribution = universal profile links, sender-only, no friend/stranger gating** — safe because no surface shows a per-author avatar to strangers and the profile page is relationship-gated.
- **House author shipped.** [built] `source='house_authored'`, `pickHouseQuestions` (called from the live queue at `queue-orchestrator.ts:269`), `buildHouseSlot`, smoke script. The conformance audit’s §3.1 “UNBUILT” is **stale**.
- **House-origin questions never render peer copy (Invariant H-1).** [built, regression-tested] **Audit correction: this is closed, not open.** `'A friend'` cannot reach house questions — house questions are non-feed-eligible (`visibility.ts:64,78`) so the unguarded fallback is unreachable, and where house renders, author resolution is centralized in `resolveAuthorDisplay` (house→`'Joshing'`, ending in `assertNever`). Asserted by `questions-types.test.ts:51-54` and `GameplayChat.house.test.tsx:46`.
- **`/replay` is removed; `/archive` is built but deliberately unlinked.** [built] `/replay` (page, `/api/replay/*` routes, `src/server/replay/`, `src/server/db/queries/replay.ts`, `ReplaySummary`) was deleted 2026-06-21 as a relic of the old system — it had no inbound links left except the ceremony "View all discoveries →" deep-link, which was removed with it. `/archive` remains built with zero inbound links by design (`PRD-D-0` §2), not dead code; the open question there is only whether to revive/delete. *(Caveat: `archive` still renders live thumbs buttons — see the `surfacePriorityScore` entry.)*
- **Home is a budgeted edition, not a log.** ⚠️ **SUPERSEDED FOR HOME by `D-HOME-DASHBOARD-MODEL-01` (2026-06-15)** — Home is now a bounded 7-day dashboard, not a budgeted edition (see the next entry). The mechanisms below remain accurate as the *as-built starting point* the dashboard slate edits: the served caps, the hero/panel/composer chrome, and the silent refill-from-queue all carry forward; what changes is the bounding instrument (window, not budget), the temporal/empty treatment (band label + per-section honest empties, §4/§9 reversed), and spent-card lifecycle (§Q4 reversed, exhausted bundles hidden). [built, cut-1] `D-HOME-PACING-01` ratified; the budget edition is live (`src/server/home/select-edition.ts`, `build-edition.ts`, rendered via `FeedList` with a `budget` prop). Caps: Direct 3 / Playables 4 / Texture 8; single `TodaysFiveCard` hero; one rotating panel; composer footer. Serve-and-overflow: direct → `/for-you`, playables → `/from-friends`, both back-navigable. **Refill-from-queue is silent and confirmed in code:** Home and subpages read the same queries; answering calls `router.refresh()` and Home re-windows top-N — no ghost states, no manual clear (`OverflowSubpageSync.test.tsx`, `FeedListBudget.test.tsx`). Temporal archive removed from the budgeted path (`groupItemsByRecency` runs only on the logged-out `!budget` branch). *Resolves the canon’s §9 item 6.* **Drift note:** `PersonActivityCard` / `groupActivityByFriend` are still imported in `FeedList.tsx` but inert on the budgeted home (`groupableFriendId` returns null for feed/milestone rows) — behavior is correct, but the symbols are present-but-defanged, not removed.
- **Home is a bounded 7-day dashboard.** [decided 2026-06-15; `-02`/`-03`/`-04`/`-05` built, `-06` pending] `D-HOME-DASHBOARD-MODEL-01` ratified; **supersedes `D-HOME-PACING-01`** (the budgeted-edition model is retired for Home). The bounding instrument is a **rolling now − 7-day window over Zone 2** (the ambient band: From Friends, Recent Activity, Shared Ground/convergence), not a budget — served caps stay (Direct 3 / Playables 4 / Texture 8; the audit's 5/5/6 cap change is **withdrawn**) but are no longer the primary bound. Zone 1 (directed "For You") is **unwindowed** — a sent question is a gift that persists until played/dismissed, pins above the band, and its `/for-you` overflow stays all-time. The band carries a single **"Past 7 days"** label stated once (per-section headings dropped/demoted); each Zone-2 section shows a **per-section honest empty state** when empty-in-window rather than being hidden or backfilled (reverses `D-HOME-PACING-01 §4`/`§9`). Exhausted From Friends bundles are **hidden once all questions are played** (shows "N of M" while answerable) — partial reversal of `D-FEED-FRIEND-ACTIVITY-01 §Q4`. **"View more" portals** (`/from-friends`, `/activities`) are bounded to the same 7 days; the standalone Activities tab (`/api/activities` → `getActivitiesForUser`, PRD §8.15) keeps its own 90-day cutoff. **Cold-start exception:** for a user with friend count ≤ 1, Zone 2 relaxes the window to the inviter's content and the band label is **suppressed** (it must not assert a false 7-day promise on an unbounded page); re-engages automatically at the 2nd friend. **Build state on this branch:** `B-HOME-WINDOW-02` (window + recent-or-nothing `boundTexture` + per-section empty signal), `B-HOME-OVERFLOW-WINDOW-03` (overflow portals), `B-HOME-BAND-LABEL-04` (band label + honest-empty rendering), and `B-HOME-EXHAUST-05` (exhausted bundles hidden per-viewer; remaining-count display) are merged; `B-HOME-COLDSTART-06` (window relaxation + label suppression via the `bandLabelVisible` seam) is pending. The "N of M questions" label shows the REMAINING (unplayed) count, in lockstep with the solid bundle triangles. Grounded in `_docs/HOME-DASHBOARD-AUDIT-01.md`.
- **“From Friends” is a chronological activity log.** [built, cut-1] Live: `/from-friends` → `getFriendActivity` → `deriveFriendActivity`, recency-ordered. Replaces the deep/breadth domain-grouped mastery summary (the old `deriveLatelyMilestones` still exists but has no production call site here). Context-routed burst grouping, held-singles, static cards, inline play (`PendingPlayablesList` → `InlineAnswerFlow`) all present. As-built sub-decisions: burst gap **45 min**; held-singles **pure re-derivation, no table** (solo release 5d, flush-to-mixed at 2 singles, 35d window); completed cards roll off at **30 days**, no history view *(⚠️ §Q4 "spent cards stay 30 days" is overridden for the fully-exhausted case by `D-HOME-DASHBOARD-MODEL-01 §3`, built in `B-HOME-EXHAUST-05`: a bundle the viewer has fully played is dropped in `build-stream.ts` on exhaustion, not on the 30-day timer — so the timer no longer governs home From Friends. There is no discrete roll-off job to remove; `deriveFriendActivity` is read-time, bounded by the scan window, so the timer is moot, not deleted)*; copy is **one shared lead set, not per-context** (context is computed but unused). ✅ **Resolved (2026-06-26):** the lead copy that once carried competition-register phrases (“on a roll,” “on a streak,” “on a tear,” “killing it”) was swapped to discovery register; both arrays live at `activity-stream.ts:883-901`. (`B-FROMFRIENDS-COPY-REGISTER-01`.)
- **Authored-question categorization reconciles against existing domains.** [built, flag-off] **Audit caveat: ships behind `RECONCILE_AUTHORED_DOMAINS`, flag-OFF (shadow-log only) — the fold is computed but not yet adopted.** When on, the authored path reconciles using **both** `convergeDomain` (pg_trgm `similarity()`) and `reconcileProposedDomain` (Haiku), sequential/short-circuiting; the fold is **silent** (no client-facing confirm; `category_overridden` appears in zero `.tsx`). Reuses the shared helpers, not a reimplementation. Decided against and confirmed out: no middle tier, no parent/child edge or roll-up, no schema change. Generated/onboarding paths untouched. *(The `CATEGORY-HIERARCHY-FINDINGS-01.md` “categorizes blind” claim is stale.)*
- **Directed-question card: gold-sans eyebrow on the plain bordered tile.** [built] The triangle-mat frame on directly-sent cards is retired (`DirectSentCard` uses `variant="bordered"`), and direct sends are marked out by an eyebrow rather than chrome: gold sans **“Sent directly to you”** in `--accent-gold` (#d9a82e), `font-semibold` (`DirectSentCard.tsx:47-49`). There is **no left edge-bar** — `SparkleEnvelope` never threads `accentColor`, so `FeedCardShell.accentBar` stays null. Broadcast (“added a question”) cards stay the plain default with no eyebrow. **This is canon** (`D-FEED-DIRECTED-CARD-RECONCILE-01`, resolved Option A 2026-06-13): the originally-specced amber/`HILITE` left edge-bar + Courier “SENT TO YOU” eyebrow were provisional paper ideas that did not ship and are not being built — the gold-sans eyebrow reads as warm-and-distinct enough on its own, and keeping `HILITE` Lately-only avoids extending that token to a second surface. The Friend Play edge-bar sub-question is moot and struck — no Friend Play card renders an edge-bar on the feed surface at all.
- **Color system: grading and category colors are to be separated by construction.** [decided, NOT built] **Audit correction — the decision stands but the build never landed; the collision is still live in the default.** Decided: WRONG moves to a true red out of the terracotta family; category color = top-level domain with leaves inheriting; the mastery triangle encodes completion (fill) + is decorative-hashed, never correctness (the only correctness-colored shape is `GeometricProgress` dots, which is correct). **As-built reality:** the default `:root` still holds the colliding values — `--game-wrong-strong:#c33d14` (`globals.css:109`) vs `--cat-literature:#c0392b` (`:119`). The de-collided true-red (`#c1121f`) lives only inside an inert `:root[data-palette="proposed"]` block that **nothing in live code activates**; `PaletteToggle` was repurposed to a card-bg cycler and is still mounted marked “TESTING ONLY — remove before shipping.” `B-VISUAL-PALETTE-PROMOTE-01` / `LITERAL-TO-TOKEN-01` do **not** exist in the repo. Leaf-inheritance (`getPortraitDomainColor`) and the triangle rule *are* live. **This is the top remediation item** — see follow-up B-prompt below.
- **`surfacePriorityScore` is a dead column — for surfacing.** [built, blocked-on-thumbs-UI] Thumbs feedback does not affect feed ordering: the feed orders strictly by `desc(sourceEventAt), desc(id)`; nothing reads the column for ranking, and it’s excluded from the question view DTO. **Audit correction: it cannot simply be dropped yet** — `QuestionRatingButtons` (still POSTing to it) remains live on **two surfaces**, `games/[id]/summary/page.tsx:371` (a linked surface) and `archive/page.tsx:414` (unlinked). Daily summary was migrated to a Heart writing `questionFeedback` only. Removal sequence: rewire/remove those two button surfaces → drop the 4 `ratings.ts` writes → drop the column. (Replaces the former Open “Thumbs-up → surface priority.”)
- **The 15-item “design consistency audit” is reviewed and closed — zero net-new findings.** [decision, not code] An external generic consistency audit (15 items + a sweeping token-system + 30-day polish plan) was dispositioned in `D-CONSISTENCY-AUDIT-DISPOSITION-01.md`: 0 net-new actionable findings; the only real thread (hardcoded literals → tokens) is already owned by `B-VISUAL-TOKEN-BUDGET-01`; the rest are contradicted by canon (category colors are identity signals, blue is CTA-only, flat letterpress is intentional, card tiers/bespoke primitives deferred by decision), invented, or generic boilerplate. **Do not convert it into tickets.** Real visual-consistency work must come from a pass against live `globals.css` + the component tree, not that audit.
- **Zod-validation convention applies to structured request bodies.** [built] JSON request-body handlers validate with Zod (audit finding E, 2026-06-04). Four documented carve-outs keep their existing validators (query-param routes already coerced/clamped; `POST /api/questions` via centralized unit-tested `readCreateQuestionPayload`; `propose-interests` and `friend-invitations` emit distinct field-specific error codes a single schema would flatten; `PATCH /api/declared-interests` drops-not-rejects invalid items). Converting later is safe-but-optional; tests catch drift.
- **Weekly Reflection — warm-register build + Beat E discovery selection.** [built] 🔁 **re-audit after merge.** `B-REFLECTION-CEREMONY-01` landed the D-REFLECTION-COPY-01 copy across all five beats (A–E) in `src/app/ceremony/[ceremonyId]/page.tsx`, killed the repeating circle/title/status triad (one strong slide per beat; lead large, rest tucked behind tappable “+{n} more”), and built the real Beat E. **Beat E selection rule resolved:** missed/expired canonical answers (`masteryEvents.answerState='incorrect'`), ranked **friend-co-miss → most-recent → hash-stable (djb2)** via the pure `selectDiscoveries` in `compute-beats.ts` (co-miss mirrors `getLatelyConvergences`, flipped to incorrect), curated to 3. **Scope decision:** curated cards are **canonical-only** (bot daily slots share no question id, so co-miss is undefined and there's no canonical text to render); pure-bot discoveries are simply not surfaced (the former `View all discoveries →` deep-link to `/replay` was removed with that page on 2026-06-21). Copy is honest to the implemented signal (no softening needed). *Flag-only carry-overs:* payload field/type still named `beat6`/`Beat6Learned` (semantics now Beat E); the partial `ceremony`/`biweekly` rename (table, route, activity type) is unchanged. (Supersedes the former Open “Beat E discovery selection rule.”)

- **Account deletion honors the author's exit without confiscating third parties' proven territory.** `[built]` 🔁 **re-audit after merge.** Ratified 2026-06-15 (`D-ACCOUNT-DELETION-TERRITORY-01`; FK inventory + conformance audit in `_docs/D-ACCOUNT-DELETION-TERRITORY-01-FK-AUDIT.md`). The four assets bundled in "delete" resolve separately: **(#1)** retained answerers' mastery events + points earned off the departed author's questions are **PRESERVED** (proven territory is the answerer's evidence, not contingent on author presence); **(#2)** the authored `Question` is **TOMBSTONED** — `creator_id` nulled, `source='house_authored'`, content/`asked_count`/`correct_count` retained so difficulty + proven-territory math + §8.27 retroactive reclassification stay computable; **(#3)** the windowed §16.16 author-credit (`MASTERY_EVENTS.source_type='author_credit'`, `user_id`=author) is **REMOVED** with the author; **(#4)** the departed author's own declared+proven territory, portrait, profile are **HARD-DELETED**. Ratified forks: **A1 tombstone** (not A2 snapshot — snapshot freezes answerer portraits and diverges from §8.27); **tombstone identity = reuse `house_authored` + a new `author_deleted` boolean** on `Question` to distinguish it from genuine house content (never renders as a person, Invariant H-1); **C1** preserve convergence/Joshing-game facts for the retained user but degrade attribution to the anonymized source (C2 remove acceptable for un-surfaced events); **D re-source to house** — repoint `FeedItem.sourceUserId` to the house sentinel and render via the tombstoned question (mirrors the existing `activityItems.actorUserId='set null'` precedent), do not leave dangling. Decision **B** (author content with no retained dependency, incl. nobody banked it) hard-deletes with the account; a friend's bank row makes it a retained dependency → falls under A1. Decision **E** (departed user's own answerer credit) hard-deletes with #4; no retained aggregate reads it. **Why no `ON DELETE CASCADE`:** the territory-critical FKs (`MASTERY_EVENTS.user_id`/`question_id`, `playerMastery.user_id`, `questions.creator_id`, `FeedItem.sourceUserId`/`questionId`) are all live-RESTRICT (no `onDelete`), so a raw `User` delete is *blocked*, not silently destructive — the deletion path must be an **application-level routine** (tombstone + selective delete), never new cascades. **Build state:** nothing built. Blockers before the endpoint: add `Question.author_deleted`; write the deletion routine handling every RESTRICT FK in the audit; 🔁 re-audit live schema vs. this decision; then the pre-launch "account deletion behavior" checklist item can be checked. Sub-questions resolved in audit: `SharePortraitCard` carries no author names (no anonymization needed); answerer tiers are independent of author-credit (`scoring.ts`); tombstoned questions stay in the difficulty-calibration denominator. **As built (2026-06-15):** migration `0080_question_author_deleted` adds `Question.author_deleted` (+ partial index + instrumentation guard); `deleteUserAccount` (`src/server/db/queries/account.ts`) was rewritten from the hard-cascade — it now partitions the author's questions into tombstone vs. orphan, deletes ONLY the departed user's own `MASTERY_EVENTS` (never by `question_id`, which was the confiscation bug), nulls `answered_by_user_id` for retained convergence rows (C), and re-sources via the tombstone (`creator_id` NULL + `source='house_authored'` → `resolveQuestionAuthor` renders house, H-1 intact). **Two as-built notes:** Decision D's "re-source to house" is realized as **strip the actor cards** for `FeedItem.sourceUserId` (a real `users.id='house'` is forbidden by H-1; the question half still re-sources to house via the tombstone) — ratified 2026-06-15; and `ContentReport` (reporter/target RESTRICT, previously unhandled) is now **handled** — its `one_target` CHECK forbids nulling a vanishing target, so reports owned by the departed user or targeting a hard-deleted orphan/generated question are deleted, while reports targeting a surviving tombstone keep their link (moderation record preserved). No live integration test in CI (the query suite is DB-mocked; the raw-SQL tx isn't exercisable there) — but a runnable conformance script ships: `npm run verify:account-deletion` (`scripts/account-deletion-territory.verify.ts`) seeds the full territory scenario against a real DB, deletes the author, asserts #1–#4/B/C/D + ContentReport, and self-cleans. Run it against a real database at the 🔁 re-audit before launch.
- **One editorial serif app-wide — Playfair retired (Decision A = A1).** `[built]` `B-TYPE-SERIF-CONSOLIDATION-01`: the app collapses to a single editorial serif (Cormorant Garamond via `--font-serif`); the second serif (Playfair Display, `--font-display`) is **retired entirely**. The original F5.2 justification (Playfair = category-name italic on Portrait/PortraitCircles) no longer held — those surfaces had already migrated off `font-display`. **Phase-0 drift correction:** the prompt's inventory called `FS` (`src/components/lately/tokens.ts`) "dead, imported nowhere" — it is **not**; it had 3 live importers on the Lately surface (`activities/loading.tsx`, `activities/page.tsx` ×2), including the **46px "Lately." italic headline**, which was actually the *largest* Playfair display moment in the app (bigger than OverlapMap's ≤1.35rem). The prompt's done-when grep was scoped to `src/components/lately` and missed them. Decision A still resolved **A1** (one serif, full stop) — the category-name/display italic intent survives in Cormorant's loaded italic. **FS handling:** rather than delete `FS` (3 importers) it is **repointed to `var(--font-serif), Georgia, serif`** — kept as a reference alias like `FM`/`FF` in the same file so consumers route through one place. Migrated incidental italics: Hero subhead, RecentActivity empty state, summary ceremony kicker, OverlapMap `italicSerif`. Loader (`Playfair_Display` instance + `--font-display` variable + `${playfair.variable}`) removed from `layout.tsx`; stale F5.2 comment and `globals.css` Playfair line reconciled to "retired"; `STYLE-GUIDE-TYPE.md` display-moment rule no longer names Playfair. (2026-06-18.)
- **Interlude grounds are sanctioned, interlude-scoped palette additions.** `[built]` `B-HOME-INTERLUDE-TYPE-01` adds two new `:root` tokens for the home editorial interludes: `--interlude-sage: #ced2bf` (flat light ground for "Overlap"; deliberately distinct from the cream-derived `--editorial-sage` wash — a flat sage formerly living only inside the retired triangle PNG) and `--interlude-terracotta` (a deep warm ground for "Find friends", `color-mix(brand-orange 82% + brand-ink)` so cream type clears AA on top). The "Write" interlude inverts onto the existing `--ink` navy. A third token, `--interlude-teal`, points at the decorative triangle teal (`--tri-darkteal`) and grounds the short daily-reminder band (below). Components reference the tokens only, never raw hex. **Home interlude wiring (2026-06-19):** World Expanding is sunset (no longer feeds the panel; query + component kept for revival); the mid-feed panel is dedicated to Overlap; Find Friends is promoted to an always-on interlude rendered above the Write composer at the feed tail. **Daily-reminder band (2026-06-19):** a short full-bleed teal interlude (`DailyReminderInterlude`) sits directly under the daily-five card, inviting email-reminder opt-in. It reuses the existing reminder machinery (PATCH `/api/account/reminders`; one-tap opt-in when the email is already verified, else an inline collect→verify flow) and **shares** the opt-in/dismissal state with the daily-summary `RoundReminderCard` (`emailOptIn` / `reminderPromptDismissedAt`), so signing up or dismissing anywhere silences it everywhere — no new columns. The server (`page.tsx` `DailyReminderSection`) renders it only when the viewer is not opted in, has no pending signup, and has not dismissed; dismissal is permanent. (2026-06-19.)
- **Color system: de-collided palette is the default.** `[built]` WRONG is a true red out of the terracotta family (`--game-wrong-strong: #c1121f`), de-collided from the brand/link orange and from Literature's category mark (which moved to bordeaux `--cat-literature: #7d2c3f`); Language moved off the CORRECT green to teal (`--cat-language: #2e6e7e`). These were formerly inert behind `:root[data-palette="proposed"]`; they are now the base `:root` values in `src/app/globals.css` and the `data-palette="proposed"` block is deleted. `SharePortraitCard.tsx`'s hardcoded html2canvas scale is re-synced value-for-value (kept in lockstep by comment). Category color = top-level domain with leaf inheritance via `getPortraitDomainColor` (unchanged). (`D-DECISIONS-CONFORMANCE-01` §5; built 2026-06-13 in `B-VISUAL-PALETTE-PROMOTE-01`.)
- **Daily set canonical size = "5 + additive bonus"; bonus never enters any count.** `[built]` `B-BONUS-QUESTION-STANDARDIZE-01` (predecessor `D-BONUS-QUESTION-DISCOVERY-01`). The daily set is **five**; the +2 bonus questions (D-4 §B) are additive and never counted toward the set size — every count/denominator surface reads **5** (core), while the dot *track length* may be 5–7. **Single source of truth (D-F5):** bonus-ness is no longer re-derived ad hoc in ~5 places — one shared selector module `src/server/daily/bonus.ts` (pure functions over `QueueSlot[]` keyed on `presence_source_id`: `isBonusSlot`/`getCoreSlots`/`getBonusSlots`/`getBonusCount`/`getSlotPresence`; malformed/missing presence → treated as **core**) is the only read-time deriver. All consumers route through it (`daily/page.tsx`, `status/route.ts`, `daily-summary.ts`, `refine.ts`, `refine/derive.ts`); the queue orchestrator stays the write-time truth (`buildPresenceSlot`). **Dot track (D-F1/D-F1b):** one continuous track — core dots → **visible gap/separator** → bonus dots → label **"+{N} friend bonus"** (N dynamic; **N=0 suppresses the separator and label** for a pixel-identical 5-dot track). The bonus signal is carried by the **gap + label (real text)**, never by color (canon R3); bonus dots reuse the existing state ramp and carry distinguishing aria-labels. Live (`GeometricProgress`) and recap (`ResultDots`) tracks share the structure. **Count parity + attribution (D-F3/D-F4):** the recap header, share copy, and the "perfect/whiffed five" beats read the **core five** (X = core corrects, Y = core count, never `slots.length`); bonus attribution is a **quiet sub-line** "from {Name}'s knowledge" (extra form "from {Name} + others' knowledge"), carried by a new `QuestionRecap.bonusPresence` field — **not** `authorName` (the friend never authored or answered the bonus question; it names where the domain came from). Register locked to **"knowledge"** (not "world"); the dot-group label stays generic ("+N friend bonus"), never a named friend. **Home card** (`TodaysFiveCard`) now mirrors the same bonus dot-group (5 core → gap → bonus dots → "+N friend bonus") so it matches the recap/live tracks — fed by a new `bonusOutcomes` array on the daily-status producers — but its spoken count stays **"of 5"** (bonus never enters the count). No schema/migration change (bonus-ness stays JSONB-in-`slots`). **UI refinements (2026-06-19):** the dot-group label sits inline beside the bonus dots; the recap header drops the visible "X / 5" readout in favor of the Share button (the score still travels in the share text); label copy settled as **"+N friend bonus"**. (2026-06-19.) **AMENDED 2026-09-06 (`D-GAMEPLAY-BONUS-COUNT-01`):** the gameplay number marker now counts the bonus run as `BONUS n of N` (see the marker entry below). The rule this entry sets — bonus never enters the CORE count, every core denominator reads **5** — is unchanged and still holds: `n of N` is a separate sequence over bonus slots only. The dot track, its `+N friend bonus` label, home-card copy, and recap counts are untouched.
- **Gameplay question number marker: above-card, solid-ink, stateless, ✦ for bonus.** `[built]` `B-GAMEPLAY-QUESTION-NUMBER-BOX-01` (pre-palette, color-agnostic — **not** gated on `B-VISUAL-PALETTE-PROMOTE-01`). A large editorial number marker (`QuestionNumberMarker`) sits in the gutter **above** each Daily Five question card during gameplay, wired through the `question` `ChatMessage`'s optional `numberMarker` ({ value, bonus }) → `QuestionRow` → `daily/page.tsx` (core slots number `slot_index + 1`; bonus via `isBonusSlot`). **(D-GAMEPLAY-NUMBER-BOX-PLACEMENT)** Above-card, reversed out of a solid `--ink` box (numeral in `--primary-foreground`, Cormorant Garamond 600 ~46px, radius ≈ 0.22×height) — justified by **legibility** against the full-strength triangle-pattern background (`/images/Variant4.png`), where a bare/colored numeral would lose contrast; this is a legibility decision, **not** a status-color one, and it does **not** re-import the retired triangle motif as a functional signal. **(D-GAMEPLAY-NUMBER-BOX-STATELESS)** The box is **positional only** — it never encodes result state (that's the dot track's job) and **persists unchanged after answering** (F2: no dim/recede/recolor). "Recolor to result" explicitly rejected. **(D-GAMEPLAY-BONUS-GLYPH)** Bonus questions render **`✦`** (square box, no period, no numeral), **never** a numeral like `6`/`7` — consistent with the "5 + additive bonus, never in the denominator" model (`B-BONUS-QUESTION-STANDARDIZE-01`). **Token notes (no new colors):** uses only existing tokens — `--ink` (fill), `--primary-foreground` (the reversed "paper" cream this codebase already pairs with navy ink; no `--paper` token exists here), and `--shadow-overlay` for the lift (the heaviest existing elevation register — chosen over a one-off rgba so no new color is introduced). **Codebase-vs-prototype reconciliation:** the prototype's per-bubble `"… · Q1 OF 5"` sub-eyebrow **does not exist** in `Joshing-11` (`grep -rn "OF 5" src` is empty), so there was nothing to remove; the category label already lives inside the card as its badge chip (`questionBadges`) and is left untouched, independent of the marker. Header dot track / "+N friend bonus" copy (`GeometricProgress`) untouched. **Register:** the question-bubble chrome change shipped with the build (no sign-off gate outstanding). (`D-GAMEPLAY-NUMBER-BOX-PLACEMENT` / `-STATELESS` / `D-GAMEPLAY-BONUS-GLYPH`; built 2026-06-22, this branch.) **AMENDED 2026-09-06 (`D-GAMEPLAY-BONUS-COUNT-01`, Josh):** the bonus marker now renders **`BONUS` over `n of N`** instead of the bare `✦`. The glyph said a bonus existed but not how many remained; the count is the point. **This does NOT reopen the denominator rule** — `n of N` is the bonus run's OWN sequence (`getBonusSlots` order), the core five still read 1–5 and every core count/denominator surface still reads 5, and `6`/`7` remains forbidden. **Return slots keep `✦`**: additive but not bonus, with no run of their own to count. **Colour:** the bonus box keeps the core `--ink` fill and adds the same **4px `--accent-gold` inset rail** the bonus CARD already ships, so marker and card read as kin (no new colour). A gold-tinted FILL was built first and rejected on inspection — mixing gold into near-black navy lands on a muddy olive-slate rather than anything recognisably gold. This is a knowing, narrow amendment to canon R3's "never by colour": colour REINFORCES here and never carries, because the literal word `BONUS` and the count do that — a monochrome or colour-blind reader loses nothing. The box stays positional and still never encodes result state (`-STATELESS` intact).
- **Answer-point scoring is centralized in one function; recovery is the multiply path; catch-up recovery does not stack.** `[decided 2026-06-26; building this branch]` Ratified `D-RECOVERY-SCORING-UNIFY-01` (implements `B-SCORING-CONSOLIDATE-01`). Five live answer routes (daily, catch-up ×2, feed, lately, questions) historically computed answer points two different ways that agreed only by a rounding coincidence: daily/catch-up used `round(first_correct_base × RECOVERY_STATE_WEIGHT)` (the multiply path), while feed/lately/questions read the separate `first_correct_after_wrong` table column. Editing `RECOVERY_STATE_WEIGHT` (`src/server/mastery/constants.ts`) or any `first_correct_after_wrong` value would silently desync daily vs feed for the same recovered question. Ratified forks: **(A1)** the **multiply path is canonical** — recovery = `round(getBasePoints(diff,'first_correct') × RECOVERY_STATE_WEIGHT)`; `RECOVERY_STATE_WEIGHT` is the single knob and the `first_correct_after_wrong` columns are now derived/asserted, not authoritative. **(B1)** all five routes route their `pointsFor` through the one helper `canonicalPointsForAnswer` (`src/lib/game-constants.ts`), passing each route's existing difficulty resolution (unchanged), its `answerState`, and `catchUp: true` only on the catch-up route. **(C1)** a per-tier invariant unit test pins `round(first_correct × RECOVERY_STATE_WEIGHT) === first_correct_after_wrong` for every difficulty so the multiply path and the table column can never silently desync. **(D1 — newly surfaced during the build, decided 2026-06-26)** the helper as originally written read the `first_correct_after_wrong` column and then *also* applied the catch-up weight, yielding **6.25%** of live base on catch-up recovery (6/3/1) — but the shipped catch-up route deliberately awards **25%** of full live base (25/13/3) and its inline comment explicitly refuses to stack ("not 6.25%"). The `constants.ts` comment claiming catch-up recovery = `CATCHUP × RECOVERY = 6.25%` was the wrong description and contradicted shipped behavior. Resolution: **preserve shipped values** — recovery earns `round(first_correct_base × RECOVERY_STATE_WEIGHT)` whether live or catch-up (recovery weight is selected *instead of*, never stacked *on top of*, the catch-up surface weight), the helper body was rewritten to the A1 multiply path so this holds, and the contradictory `constants.ts` comment was corrected. **Player-visible point values are byte-identical before/after** across the full (difficulty × answerState × catchUp) matrix — this PR centralizes computation only; it does **not** re-tune scoring. `POINTS_PER_CORRECT` (legacy flat `3`, zero importers) is retired. (`D-RECOVERY-SCORING-UNIFY-01`; `B-SCORING-CONSOLIDATE-01`, this branch.)
- **From Friends milestone bundles: bundle-as-header, one card per aced question (Option C).** `[decided, NOT built]` Ratified 2026-06-22 (`D-FROMFRIENDS-STREAK-HEADER-01`; read-only audit confirmed §2.1–2.5 against live code). The collapsed "Play" bundle card (a one-liner + "{remaining} of {total} questions" + a single Play disclosure, `ActivityStreamItem` `editorialCard`/`playableCard` branch) is replaced so the directed and From-Friends paths read as **one card language**. **(D-A) Render model:** the streak line becomes a lightweight section **header** (attribution + context, rendered once), and **each aced question renders as its own answer/dismiss card sharing the `DirectSentCard` register** — elevated cream fill (`--feed-card-elevated`), hairline `--brand-border`, primary filled **Answer** button, **Dismiss** link (`SparkleEnvelope` `variant="bordered"`, `answerAsButton`, `onAnswer`/`onDismiss`). No new color-bearing tokens — reuses tokens already live in the feed, so **not gated on** `B-VISUAL-PALETTE-PROMOTE-01`. **(D-B) Budget unit = whole bundles, never split; cap 4→5.** `PLAYABLE_SERVE_CAP` 4→**5** in `select-edition.ts` — a **bundle** cap applied via `serve(orderFriendActivity(...), …)`; a friend's streak is served intact or rolls to `/from-friends` overflow, never severed mid-streak (`serve()` slices the ordered bundle list, never a question list). Within a served bundle **all** aced questions render (natural max 5 → worst case 5×5 = 25 cards + 5 headers). No per-question budgeting; the per-bundle soft cap is **deferred** (revisit only if a single streak dominates the scroll at ~12 users). Overflow counts **bundles** ("N more →"), unchanged. **⚠️ Ratification tension to resolve:** `D-HOME-DASHBOARD-MODEL-01` (2026-06-15) explicitly **withdrew** "the audit's 5/5/6 cap change" and states "served caps stay (… Playables 4 …)". D-B re-opens **only** the Playables cap (4→5), scoped to this header/card render, leaving Direct 3 / Texture 8 as withdrawn. This must be ratified as a deliberate re-open of that one constant, not an oversight — flag for the cap owner before the B-prompt. **(D-C) Fork F1 — single-question bundles get no header.** A 1-question bundle suppresses the streak header (header + one card is heavier than the row it replaces) and the lone card carries a compact inline attribution — "**via {friend}**'s streak" with the hourglass mark (reuse `HourglassMark` + `viaAttribution`). Header renders only at bundle size **≥ 2**. **(D-D) Per-card provenance honesty preserved.** Each un-collapsed question card must render honest provenance (human vs house/LLM) exactly as the broadcast/directed paths do — no "A friend" fallback for machine content (canon tripwire, Invariant H-1). **Data is present, not a gap:** milestone `StreamQuestion` already carries `authorName`/`authorIsHouse` (`build-stream.ts:124-125`), threaded identically to the convergence/your-question paths. **Render thread to ADD:** today the milestone path shows provenance only **post-answer** (`AnswerFeedbackSheet`, `InlineAnswerFlow.tsx:181-182`); the **pre-answer** `InlineAnswerFlow` render (`:120-157`) shows the question text with **no** provenance marker, unlike `SendOnwardExpansion` which renders `QuestionProvenance` from the carried fields up front (`ActivityStreamItem.tsx:939`). Option C exposes each question pre-answer, so the B-prompt MUST render the honest marker on the un-collapsed card via the existing `questionProvenance`/`QuestionProvenance` helper. **Preserve, don't drop:** the solid→hollow triangle answered-state currently owned by `ActivityStreamItem` (`serverAnswered`/`resolutions`/`isResolved`, `:229-260`) must be relocated or preserved **per-card** (§2.5). **Open copy-register call (→ Robyn): F-COUNT** — does the header state how many were aced? Recommend **(a)** no count (the cards are the count) over **(b)** a quiet "aced N" / dot track. Plus two Robyn register checks: the streak line standing alone as a header (still her voice?) and the F1 "via {friend}'s streak" line. **Honesty note (load-bearing):** the header attributes a *friend's* proven mastery — it must never read as machine-authored. (`D-FROMFRIENDS-STREAK-HEADER-01`; grounded in the §2 audit, this branch.)

## Open — pick these up

- **Human-authored-pivot exploration — record only, un-ratified.** `[thinking-record]` A design session explored inverting content authority (human authors question sets; the LLM demoted to staff) with finite sets, performance-based mastery, and a contribution→mastery loop. Captured under `docs/thinking/` (start at `SESSION-SUMMARY-human-authored-pivot.md`; reading order in `docs/thinking/README.md`). Nothing there is ratified or a build instruction, and it gates on the `PRE-BUILD-VALIDATION` checks. **The supply pause (`CC-SUPPLY-HALT-01`) still holds** — the exploration reinforces it, does not lift it.
- **Land the palette promotion for real.** [highest priority] The grading/category collision is **live in the default** (`#c33d14` vs `#c0392b`); the fix is inert behind `data-palette="proposed"`. Promote proposed→default, retire the still-mounted `PaletteToggle`, **and** re-sync `SharePortraitCard.tsx`‘s hardcoded hex (html2canvas can’t read CSS vars — it currently mirrors the OLD scale and would drift the instant the palette flips) in the same prompt. Minor tail: `DomainCircle.tsx` still hardcodes `#d4cfc7` / `#c8c0b0`. See B-prompt `B-VISUAL-PALETTE-PROMOTE-01`.
- ~~**From Friends copy violates the discovery-register rule.**~~ **Resolved (2026-06-26)** — both lead arrays (`FRIEND_ACTIVITY_LEADS` / `FRIEND_ACTIVITY_LEADS_NO_TOPIC`, live at `activity-stream.ts:883-901`) were swapped from competition/achievement register (“on a roll / on a streak / on a tear / killing it”) to discovery/curiosity register (“has been wandering through”, “keeps finding corners in”, “has been following a thread”). Strings only — no change to `friendActivityToStreamItem`, the rotation, or the interpolation grammar; the adjacent comment now matches the register. (`B-FROMFRIENDS-COPY-REGISTER-01`.)
- **Adopt or hold `RECONCILE_AUTHORED_DOMAINS`.** The authored reconcile is built but flag-off (shadow-log only). Decide when to flip after reviewing the shadow-log duplication numbers.
- ~~**Option B: should the aside amplify a human’s `creatorNote` when one exists?**~~ **Resolved (2026-06-15) — unified.** The aside and the creator note are now ONE note (`src/components/CreatorNote.tsx`), and treatment is chosen by **provenance, never answer-state**: a human author speaks in a bold inverted INK block ("Between you and {Name}"); house/LLM render as the quiet bronze editorial aside ("Between us!"). A human question never carries a machine-generated aside — `generateInsideJoke` was removed from the authored create path (`POST /api/questions`); the daily/LLM + house generation paths are unchanged. (`B-CREATORNOTE-EDITORIAL-ASIDE-01`; supersedes `PRD-D-0` §5.)
- **Niche-match production default.** Ships `false`, test-phase `true`; production default open, revisit after the test. (`PRD-D-2`.)
- **Feed verb `'wrote this'` on broadcast.** The `authored_shared` branch claims authorship unconditionally; resolve. (Conformance audit §3.2.)
- ~~**Build account-deletion behavior.**~~ **Built (2026-06-15)** — `0080_question_author_deleted` + the rewritten `deleteUserAccount` (tombstone partition, retained-mastery preservation, C-degrade, D actor-strip). See the Settled entry above. **Remaining before the pre-launch checkbox:** run the shipped conformance script `npm run verify:account-deletion` against a *live* database (the rewrite is typecheck/lint/mock-test verified, but the raw-SQL transaction isn't exercised by the DB-mocked query suite). The script seeds the territory scenario, deletes the author, asserts the ratified behavior, and self-cleans.
- **F-COUNT (Robyn) — does the From Friends streak header state how many were aced?** `[copy-register, → Robyn]` Option C drops the explicit "{remaining} of {total} questions" text. **(a)** header carries no count (the N cards *are* the count) vs **(b)** a quiet count ("Joshua aced 5 — answer any" or a dot track). Recommend **(a)** for calm; this is a copy-voice call, not layout. Two paired register checks: the streak line now stands **alone** as a header above cards rather than introducing a Play affordance (still her voice?), and the F1 compact line "via {friend}'s streak". Gate before the `D-FROMFRIENDS-STREAK-HEADER-01` B-prompt. (Recorded 2026-06-22.)
- **Ratify the Playables cap re-open (4→5) for the streak-header render.** `[ratification]` `D-FROMFRIENDS-STREAK-HEADER-01 D-B` re-opens `PLAYABLE_SERVE_CAP` 4→5, which `D-HOME-DASHBOARD-MODEL-01` had explicitly **withdrawn** ("the audit's 5/5/6 cap change is withdrawn; … Playables 4"). Confirm this is a deliberate, scoped re-open of that one constant (Direct 3 / Texture 8 stay withdrawn) before the B-prompt edits the constant; re-verify the `select-edition` budget unit tests and add a cap=5 case. (Recorded 2026-06-22.)
- **`Via` answerer attribution ships ungated for the 12-user test phase.** `[test-phase]` `B-VIA-ATTRIBUTION-01` adds a "Via [friend]" line to feed cards (follow-gated, recency-ordered, names friends who answered a question correctly; `src/server/feed/via-answerers.ts` + `ViaAttribution.tsx`). **(a)** It is **on-by-default with no answerer consent control** — a friend cannot opt out of being named as having answered. **(b)** Unlike authorship (which is self-evidently public the moment you write a question), "Via" **leaks a friend's answering activity** — a more sensitive fact the answerer never affirmatively published. **(c) Revisit before opening the graph** beyond the trusted test cohort, and before/when a two-sided consent primitive is built — this ties directly to the consent-primitive gap flagged in `D-FRIEND-VISIBILITY-PRIVACY-AUDIT-01`. This entry is the line between "pragmatic MVP" and latent privacy debt nobody chose: shipping ungated is a deliberate test-phase call, not an oversight.

## What belongs in this file

**The trigger is a decision *not* to build something** — or a rule about *how* to build that no single
change embodies. Those are the entries with nowhere else to live: a decision to build leaves a PR, code
and tests behind, while a decision to skip work leaves nothing at all — and an omission here reads as
“never considered” rather than “considered and declined.”

## Track A / deferral decisions (2026-09-04 → 05)

Recorded here because these were **decisions not to build something**, or rules about *how* to
build, and that class has nowhere else to live: a decision to skip work leaves no code behind.
Where a rule is already durable in a comment or test, this is a pointer, not a copy.

- **`INVITE-DEFAULT-01` — new invite links default to the untagged "No category" slot.** `[decided: OUT]`
  Selection, editing, validation and top-three-by-frequency defaulting are **already live** —
  `AddTopicField` capped at three, `PATCH /api/account/invite-links/topics` validating with
  onboarding's own too-broad check, `getInviteLinkSeedTopics` ordering `often` first, panel
  prefilled so the first edit converts automatic → curated, per-link slot selection present. The
  **open item is the default slot only**: making category-scoped the primary structure is a
  data-model change behind topics. Out on *ordering*, not cost — it sat ahead of a measured ~15s
  latency win with a known mechanism, and the complaint that started it (a raw base64 URL on the
  card) is fixed. **Re-raise when** (1) the bonus deferral has shipped and p50 is re-measured, or
  (2) anyone touches topic categorisation for another reason.
  *An earlier version of this entry described the whole feature as unbuilt. It was wrong, and a
  product decision was made on it. Scope is a carried claim until something reads the code.*

- **Parallelising the bonus generation loop — rejected.** `[decided: NO]` Deferral removes the wait
  without touching the loop, so the concurrency risk buys nothing that sequencing does not.

- **Never edit an applied migration; correct forward.** `[rule]` `0135` was edited after it had been
  applied (a `CHECK` idempotency wrapper added after a failed boot). Semantically harmless, but its
  file hash no longer matches the applied row. Drizzle compares `created_at` against `folderMillis`
  and **never reads `hash`** — verified in `pg-core/dialect.cjs` and then observed on a live deploy —
  so the divergence is cosmetic. The rule stands regardless: once applied, correct forward.

- **`target_size` is INTENDED size, never ACHIEVED size.** `[built]` Written once at persist as the
  constant `DAILY_QUEUE_SIZE` — the target the fill loop, the top-up rounds and borrow-back are all
  trying to reach — and **never derived from `slots`**. Deriving it from the array (an earlier cut
  used `getCoreSlots(slots).length`) records the ACHIEVED count, so a build that misses and persists
  four core slots sets `target_size` 4 and reads as *complete at four answers* — the under-delivery
  written in as the target, certifying the short queue instead of detecting it. The two formulas are
  indistinguishable on every healthy build and diverge only on the builds the column exists to find,
  which is why nothing catches it. No build intends fewer than five: `DAILY_QUEUE_MIN_SIZE` is a
  tolerated graceful-degrade floor, not an intent. Upstream history: `0136` backfilled from *all*
  slots and wrote 7 for the modal 5-core+2-bonus queue (148 rows); `0137` set every row to NULL
  rather than a corrected number, for the same reason. The `NOT NULL DEFAULT 5` was dropped too:
  **an unwritten value must be visible, not plausible** — a rule since applied to `user_visible_ms`
  (0138), `span_ms` (0139) and the 0140 counts.
- **`getCoreSlots` marker-less-means-core serves two callers with opposite requirements.** `[rule]`
  Right for **completeness** (an extra question beats a stranded player); wrong for **analytics**
  (it silently understates bonus). `BONUS_MARKER_FLOOR = '2026-06-04'` is the first date a bonus
  marker reached a live queue — **117 of 309 queues, 38% of history, predate it** and classify as
  all-core with nothing in the data saying so. Scope any retroactive core/bonus figure to that floor
  or label it unscoped. Durable in `src/server/daily/bonus.ts`.

- **A1a was a deliberate reversal, not a bug fix.** `[built]` `isRoundComplete` now counts core slots
  only. The test it overturned was added 2026-06-01 in the +2 commit itself, using the then-current
  `answerer_id` marker, and **knowingly** asserted that an unanswered bonus slot holds the round
  open. Reversed because counting additive slots stranded the modal queue permanently — 5 real
  queues measured, and completion also gates the demand-pull bank replenish, so a stranded round
  never restocked. The block's *stated* concern ("track the actual slot count, not a fixed five") is
  preserved: skip replacements carry no marker, so they remain core.

- **`ALLOW_REMOTE_BOOT_MIGRATE` stays unset on Preview.** `[decided]` The refusal fired live in
  preview naming host `aws-1-us-west-2.pooler.supabase.com`, vs production's
  `db.<project-ref>.supabase.co`. A different hostname is **not** evidence of a different database:
  that pooler endpoint is shared and multiplexes projects by the username, which the guard
  deliberately never logs. Most likely the same database via the pooled endpoint — recorded as
  **inference, not observation**. Fail toward the recoverable error: if preview shares prod, setting
  the flag recreates the 2026-09-03 incident from CI automatically; if preview is isolated, the only
  cost is schema drift in a throwaway environment. Preview is additionally behind Vercel SSO, so
  booting one requires an authenticated team member. See `_docs/INCIDENT-2026-09-03-boot-migrate.md`.

- **A deploy does not apply migrations; a request does.** `[rule]` A production deployment can sit
  READY with `register()` never having run, because Vercel does not boot a function until traffic
  arrives — observed on the `#1597` deploy, which applied nothing until a `GET /login`. **Never read
  READY as evidence that a migration applied.** It also sharpens the incident: production sits cold,
  so a local process pointed at prod is often the *only* thing running `register()` at all.

## Known-stale source claims

Corrected at the source when next editing those files:

- **Conformance audit §3.1 — “house author UNBUILT.”** Stale; house author is live.
- **`D-HOME-PACING-01.md` header — “not yet sequenced into build prompts.”** Stale; built cut-1 and live.
- **`src/lib/friend-activity.ts:1-6` — “SKETCH / not yet wired in.”** Stale; wired and live.
- **`CATEGORY-HIERARCHY-FINDINGS-01.md` — authored path “categorizes blind.”** Stale; reconcile path exists.
- **`PRODUCT-CANON.md` §9 item 6 — Home-pacing-vs-tiers sequencing “unresolved.”** Stale; sequenced and built.
