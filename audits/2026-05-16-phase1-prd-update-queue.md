# Joshing PRD Update Queue — v11.1 → v11.2

_Date: 2026-05-16 · Scope: every PRD-vs-code divergence found in Phase 1 triage, regardless of severity._
_High-severity items (#1–5) also appear in Artifact A's Top-10. Lower-severity items live only here._

---

| # | PRD section | Code location | What PRD says | What code does | Likely fix side | Severity |
|---|-------------|---------------|---------------|----------------|-----------------|----------|
| 1 | §8.32 — Author credit rule | `src/server/mastery/scoring.ts:70–94`; `src/server/mastery/constants.ts:36–41` | 0.5× calibrated difficulty, awarded only on Moderate/Specialist questions, one credit per question per answering player ever | Empirical-rate windowed scheme (25/50/100 pts base; full credit for first 2–5 correct globally, half for next 2–5, zero after); no difficulty filter; `AUTHOR_CREDIT_WEIGHT = 0.5` defined but unused | discuss | P1 |
| 2 | §8.1.x — Ceremony model | `src/server/ceremony/fire-ceremony.ts`; `src/app/api/cron/biweekly-ceremony/route.ts` | PRD described two-act per-game ceremony (out of date) | **RESOLVED 2026-05-16 — code is correct, PRD is stale.** Ceremony is biweekly-personal. PRD §8.1.x ceremony sections must be rewritten to describe the biweekly personal cron model; remove all Act 1 / Act 2 and per-game trigger text. | PRD | — |
| 3 | §8.32 — Catch-up + recovery combination | `src/app/api/daily/catchup/answer/route.ts:122–128`; `src/server/mastery/constants.ts:24–32` | PRD specifies catch-up = 25% of live base and recovery (first_correct_after_wrong) = 25%; silent on compound case | Code compounds: catch-up recovery = CATCHUP_WEIGHT × RECOVERY_WEIGHT = 6.25% of live base | discuss | P2 |
| 4 | §8.4.8 — Knowledge page circle sizing (tier-anchored) | `src/app/knowledge/page.tsx` | Establishing 18–28px, Familiar 32–48px, Solid 52–72px, Mastery 76–96px; tier matters first, points within tier second | Not verified at implementation level in Phase 1; requires Phase 2 check | either | P2 |
| 5 | §8.1.14 — Quip stored on answer record | `src/app/api/daily/answer/route.ts:218–220` | "Quips are selected server-side at grade time and stored on the answer record" | Daily quips stored in JSONB queue slot (not a typed DB column); Feed and JoshingGame have typed `quip` column ✓ | PRD | P3 |
| 6 | §10 — `feed_items.source_result` data type | `src/server/db/schema.ts:683` | `source_result enum(correct, incorrect), nullable` | `sourceResult: text('sourceResult')` — unconstrained text, no DB-level enum validation | code | P3 |
| 7 | §8.2 / §8.5.2 — `authored_shared` killed at application layer | `src/server/feed/visibility.ts:6`; `src/server/db/queries/feed.ts:265`; `src/server/db/queries/activity.ts:543` | "`authored_shared` is no longer written by application code"; enum value persists in schema for migration safety only | `AUTHORED_SHARED_FEED_SOURCE_TYPE` exported as live constant; included in active feed query filters as legacy support; no new writes confirmed, but export signals it is still in use | code (cleanup) | P3 |
| 8 | §8.1.11 — Thumbs-up is quality signal only, does not propagate | `src/app/api/feed/[feedItemId]/thumbsup/route.ts` | Thumbs-up records a quality signal and feeds surface priority; does NOT create feed items or propagate questions | Correctly records signal and increments `surfacePriorityScore` only; no feed-item creation ✓ | n/a — code matches PRD | — |
| 9 | §8.2.3 — Feed propagation model (friend-answered) | `src/server/feed/create-feed-items-for-answer.ts`; all three answer routes | Questions enter a player's Feed when a friend answers them (correct or wrong) in any context | `createFeedItemsForFriendsFromAnswer` called from daily, catchup, and feed answer routes ✓ | n/a — code matches PRD | — |
| 10 | §8.2.11 — "Not my focus" domain dismissal | `src/server/db/schema.ts` (`FeedDismissedDomain`); `/api/feed/dismiss-domain`; `/api/feed/dismissed-domains` | Permanent domain-level feed filter; reversible from Knowledge page | Implemented: `feedDismissedDomains` table with `dismissed_at` / `reinstated_at`; dismiss and restore routes present ✓ | n/a — code matches PRD | — |
| 11 | §7.3 — Onboarding cultural anchor | `src/app/onboarding/OnboardingFlow.tsx:179–225`; `/api/onboarding/propose-interests` | Birth year + geography → LLM generates hyper-specific culturally-anchored candidate interests; fallback to warm-up answers alone if geography insufficient | `birth_year`, `grew_up_country`, `grew_up_region` captured in UI; `propose-interests` route exists; LLM integration not verified at implementation level in Phase 1 | either — requires Phase 2 check | — |
| 12 | §8.4.3 — Territory type (declared / demonstrated) | `src/server/db/schema.ts:309` | `territory_type enum(declared, demonstrated)` on KB domains; authorship opens declared; friend-mediated correct answer promotes to demonstrated | `territoryType: text.$type<'declared' \| 'demonstrated'>().default('demonstrated')` — text field with type cast (not a true DB enum); `promoteDeclaredToDemonstrated` called from all correct-answer paths ✓ | code (consider proper enum) | P3 |
| 13 | §8.4.11 — "Grow your map" section | `src/app/knowledge/page.tsx:502` | Quiet card at bottom of Knowledge page with expansion CTA and two action buttons | Implemented with matching heading copy ✓ | n/a — code matches PRD | — |
| 14 | Tier names — audit brief vs. PRD v11.1 and code | `src/server/mastery/tiers.ts:4–9`; `src/types/db.ts:16–20` | Audit brief says "Curious → Versed → Fluent → Master"; PRD v11.1 §8.4.8 says "Establishing / Familiar / Solid / Mastery" | Code uses Establishing / Familiar / Solid / Mastery — matches PRD v11.1, not the audit brief | PRD (audit brief is stale; v11.2 should confirm canonical tier names) | P3 |
| 15 | §8.9.2 — Intellectual Alignment inputs | Not yet verified at implementation level | Alignment inputs: questions A wrote that B answered correctly; domains both demonstrated; weighted by depth. Thumbs-up explicitly excluded. | Requires Phase 2 read of the alignment calculation | either — requires Phase 2 check | — |

---

_Items marked "n/a — code matches PRD" are included for completeness; they confirm divergence was checked and resolved. Phase 2 should revisit items marked "requires Phase 2 check."_
